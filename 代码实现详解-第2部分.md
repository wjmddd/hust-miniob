# 💻 MiniOB 16个功能代码实现详解（第二部分）

> 接续 CODE_IMPLEMENTATION_DETAILS.md

## 5. date - 日期类型

### 📝 功能概述

支持 DATE 类型字段：
- 存储格式：'YYYY-MM-DD'
- 范围：1970-01-01 到 2038-02-XX
- 验证：闰年、月份天数
- 支持比较和索引

### 🗂️ 关键文件

```
Parser:   lex_sql.l, yacc_sql.y
Type:     src/observer/common/type/date_type.cpp
Storage:  table.cpp, bplus_tree.cpp
```

### 🐛 调试指南 - DATE 类型

#### 断点位置设置

**调试 DATE 类型的关键断点**：

1. **词法分析**：查看生成的 `lex.yy.c`
   ```
   断点：识别 DATE_STR 的规则处理代码
   ```

2. **语法解析**：查看生成的 `yacc_sql.cpp`
   ```
   断点：value 规则中处理 DATE_STR 的分支
   ```

3. **日期类型**：`src/observer/common/type/date_type.cpp`
   ```
   断点1：DateType::parse_date() 函数开始
   断点2：闰年判断 is_leap_year()
   断点3：天数计算 days_in_month()
   断点4：日期转整数 date_to_int()
   断点5：整数转日期 int_to_date_string()
   ```

4. **比较操作**：`src/observer/common/type/date_type.cpp`
   ```
   断点：DateType::compare() 方法
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE TABLE events(id int, event_date date);
INSERT INTO events VALUES (1, '2024-01-15');
INSERT INTO events VALUES (2, '2024-02-29');  -- 测试闰年
SELECT * FROM events WHERE event_date > '2024-01-01';
```

**调试场景1：解析日期字符串**
```cpp
// 在 DateType::parse_date() 中
RC DateType::parse_date(const char *s, int &date_value)
{
  int year, month, day;
  
  // ← 断点：查看输入字符串 s 的内容
  if (sscanf(s, "%d-%d-%d", &year, &month, &day) != 3) {  // ← F10单步
    // 检查是否成功解析
    LOG_WARN("invalid date format: %s", s);
    return RC::INVALID_ARGUMENT;
  }
  
  // ← 断点：检查 year, month, day 的值
  // 例如：year=2024, month=2, day=29
  
  // 范围检查
  if (year < 1970 || year > 2038) {  // ← 断点：测试边界
    return RC::INVALID_ARGUMENT;
  }
  
  if (month < 1 || month > 12) {  // ← 断点
    return RC::INVALID_ARGUMENT;
  }
  
  // 天数检查（考虑闰年）
  int max_day = days_in_month(year, month);  // ← F11 进入函数
  // 检查 max_day 的值，对于2024年2月应该是29
  
  if (day < 1 || day > max_day) {  // ← 断点
    return RC::INVALID_ARGUMENT;
  }
  
  // 转换为整数
  date_value = date_to_int(year, month, day);  // ← F11 进入
  // 检查 date_value 的结果
  
  return RC::SUCCESS;
}
```

**调试场景2：闰年判断**
```cpp
// 在 DateType::is_leap_year() 中
bool is_leap_year(int year)  // ← 断点，检查 year 参数
{
  // 能被4整除且不能被100整除，或者能被400整除
  return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
  
  // 测试用例：
  // 2024 % 4 = 0, 2024 % 100 = 24 → true (闰年)
  // 2100 % 4 = 0, 2100 % 100 = 0, 2100 % 400 = 100 → false (非闰年)
  // 2000 % 400 = 0 → true (闰年)
}
```

**调试场景3：日期转整数**
```cpp
// 在 DateType::date_to_int() 中
int date_to_int(int year, int month, int day)
{
  int days = 0;  // ← Watch 变量：观察累积过程
  
  // ← 断点：开始计算
  // 例如：2024-02-29
  
  // 加上整年的天数 (1970 到 2023)
  for (int y = 1970; y < year; y++) {  // ← 断点
    days += is_leap_year(y) ? 366 : 365;
    // Watch: days 值逐渐增加
  }
  // 循环结束后，days = 1970-01-01 到 2023-12-31 的天数
  
  // 加上当年已过月份的天数 (1月)
  for (int m = 1; m < month; m++) {  // ← 断点
    days += days_in_month(year, m);
    // m=1: days += 31 (1月)
    // Watch: days 继续增加
  }
  
  // 加上当月的天数
  days += day;  // ← 断点：最后加上29
  // 最终 days 值
  
  return days;
}
```

**调试场景4：日期比较**
```cpp
// 在查询 WHERE event_date > '2024-01-01' 时

// PredicatePhysicalOperator::next() 中
Value left_value;  // 从记录中读取的 event_date
Value right_value; // 常量 '2024-01-01'

// ← 断点：查看两个值
// left_value.get_int()  = 19767 (假设)
// right_value.get_int() = 19724 (2024-01-01)

int cmp = left_value.compare(right_value);  // ← F11 进入

// 在 DateType::compare() 中
int compare(const Value &left, const Value &right) const override
{
  int left_date = left.get_int();   // ← 19767
  int right_date = right.get_int(); // ← 19724
  return left_date - right_date;    // ← 43 (正数，表示 left > right)
}
```

**Watch 窗口观察**：
```
s                    // 输入字符串 "2024-02-29"
year, month, day     // 解析结果
max_day              // 该月最大天数
date_value           // 转换后的整数值
days                 // date_to_int() 中的累积天数
```

**调试技巧 - 测试边界情况**：
```cpp
// 设置条件断点
// 在 days_in_month() 中：month == 2
// 这样只在处理2月时中断

// 测试用例：
INSERT INTO events VALUES (1, '1970-01-01');  // 最小日期
INSERT INTO events VALUES (2, '2038-01-19');  // 最大日期
INSERT INTO events VALUES (3, '2024-02-29');  // 闰年2月29日
INSERT INTO events VALUES (4, '2023-02-29');  // 应该失败（非闰年）
```

### 💻 代码详解

#### 5.1 词法和语法解析

**lex_sql.l**：

```lex
/* 日期字符串: '2024-01-01' */
{QUOTE}([0-9]{4}'-'[0-9]{1,2}'-'[0-9]{1,2}){QUOTE} {
    yylval->string = strdup(yytext);
    return DATE_STR;
}
```

**yacc_sql.y**：

```yacc
type:
    DATE {$$ = static_cast<int>(AttrType::DATE);}
    ;

value:
    DATE_STR {
        char *tmp = common::substr($1, 1, strlen($1) - 2);
        $$ = new Value();
        
        // 解析日期字符串
        int date_value;
        RC rc = DateType::parse_date(tmp, date_value);
        if (rc != RC::SUCCESS) {
            $$->set_type(AttrType::UNDEFINED);
        } else {
            $$->set_date(date_value);  // 存储为整数
        }
        free(tmp);
        free($1);
    }
    ;
```

#### 5.2 日期类型实现

**date_type.cpp**：

```cpp
class DateType : public DataType
{
public:
  // 解析日期字符串
  static RC parse_date(const char *s, int &date_value)
  {
    int year, month, day;
    
    // 1. 解析年月日
    if (sscanf(s, "%d-%d-%d", &year, &month, &day) != 3) {
      LOG_WARN("invalid date format: %s", s);
      return RC::INVALID_ARGUMENT;
    }

    // 2. 范围检查
    if (year < 1970 || year > 2038) {
      return RC::INVALID_ARGUMENT;
    }

    // 3. 月份检查
    if (month < 1 || month > 12) {
      return RC::INVALID_ARGUMENT;
    }

    // 4. 天数检查（考虑闰年）
    int max_day = days_in_month(year, month);
    if (day < 1 || day > max_day) {
      return RC::INVALID_ARGUMENT;
    }

    // 5. 转换为整数（自1970-01-01以来的天数）
    date_value = date_to_int(year, month, day);
    return RC::SUCCESS;
  }

  // 判断闰年
  static bool is_leap_year(int year)
  {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
  }

  // 获取某月的天数
  static int days_in_month(int year, int month)
  {
    static const int days[] = {0, 31, 28, 31, 30, 31, 30, 
                                31, 31, 30, 31, 30, 31};
    
    if (month == 2 && is_leap_year(year)) {
      return 29;  // 闰年2月
    }
    return days[month];
  }

  // 将日期转换为整数（天数）
  static int date_to_int(int year, int month, int day)
  {
    // 计算从1970-01-01到指定日期的天数
    int days = 0;
    
    // 加上整年的天数
    for (int y = 1970; y < year; y++) {
      days += is_leap_year(y) ? 366 : 365;
    }
    
    // 加上当年已过月份的天数
    for (int m = 1; m < month; m++) {
      days += days_in_month(year, m);
    }
    
    // 加上当月的天数
    days += day;
    
    return days;
  }

  // 将整数转换为日期字符串
  static std::string int_to_date_string(int date_value)
  {
    // 反向计算年月日
    int year = 1970;
    int remaining_days = date_value;
    
    // 计算年份
    while (remaining_days > 365) {
      int days_in_year = is_leap_year(year) ? 366 : 365;
      if (remaining_days >= days_in_year) {
        remaining_days -= days_in_year;
        year++;
      } else {
        break;
      }
    }
    
    // 计算月份和日期
    int month = 1;
    while (remaining_days > days_in_month(year, month)) {
      remaining_days -= days_in_month(year, month);
      month++;
    }
    int day = remaining_days;
    
    // 格式化为字符串
    char buf[32];
    snprintf(buf, sizeof(buf), "%04d-%02d-%02d", year, month, day);
    return std::string(buf);
  }

  // 比较两个日期
  int compare(const Value &left, const Value &right) const override
  {
    int left_date = left.get_int();   // DATE存储为int
    int right_date = right.get_int();
    return left_date - right_date;
  }
};
```

#### 5.3 存储和索引

**字段元数据（field_meta.cpp）**：

```cpp
// DATE类型占4字节（int32_t）
if (attr.type == AttrType::DATE) {
    len_ = sizeof(int32_t);  // 4字节
}
```

**B+树索引**：

```cpp
// DATE类型可以直接作为B+树的键
// 因为已经转换为整数，可以直接比较
int BplusTreeIndex::compare_key(const char *key1, const char *key2)
{
  if (field_meta_.type() == AttrType::DATE) {
    int date1 = *(int *)key1;
    int date2 = *(int *)key2;
    return date1 - date2;
  }
  // ... 其他类型
}
```

---

## 6. text - 长文本类型

### 📝 功能概述

支持 TEXT 类型（最大4096字节）：
- 变长存储
- 超过4096字节自动截断
- 需要调整 record_manager 的实现

### 🗂️ 关键文件

```
Storage: record_manager.cpp, table.cpp
Type:    char_type.cpp (TEXT复用CHAR的逻辑)
```

### 🐛 调试指南 - TEXT 类型

#### 断点位置设置

**调试 TEXT 类型的关键断点**：

1. **表层**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::make_record() - 计算记录大小
   断点2：检测 TEXT 字段的分支
   断点3：text_len > 4096 的截断逻辑
   断点4：写入长度字段 (sizeof(short))
   断点5：写入实际数据
   ```

2. **记录管理**：`src/observer/storage/record/record_manager.cpp`
   ```
   断点1：insert_variable_record() 函数开始
   断点2：查找有足够空间的页面循环
   断点3：分配新页面的代码
   断点4：写入记录大小
   断点5：更新页面元数据
   ```

3. **读取记录**：
   ```
   断点：读取 TEXT 字段时如何解析长度
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE TABLE articles(id int, title text, content text);
INSERT INTO articles VALUES (1, 'Short', 'Hello');
INSERT INTO articles VALUES (2, 'Long', 'very long text...');  -- 超过4096字节
SELECT * FROM articles WHERE id = 1;
```

**调试场景1：计算变长记录大小**
```cpp
// 在 Table::make_record() 中
RC Table::make_record(int value_num, const Value *values, Record &record)
{
  // 1. 计算记录总大小
  int record_size = sizeof(int);  // ← 记录大小字段本身
  bool has_variable_field = false;
  
  // ← 断点：遍历所有字段
  for (int i = 0; i < value_num; i++) {  // ← 断点，检查 value_num
    const FieldMeta *field = table_meta_.field(i + sys_field_num);
    
    if (field->type() == AttrType::TEXT) {  // ← 断点：进入TEXT分支
      has_variable_field = true;
      
      const Value &value = values[i];
      int text_len = value.length();  // ← 检查文本长度
      
      // Watch: text_len 的值
      // 例如：text_len = 5000 (超过4096)
      
      if (text_len > 4096) {  // ← 断点：截断逻辑
        text_len = 4096;  // 截断到4096
        LOG_WARN("text truncated to 4096 bytes");
      }
      
      // TEXT 字段大小 = 2字节长度 + 实际数据
      record_size += sizeof(short) + text_len;  // ← 检查累积的大小
      
    } else {
      // 定长字段
      record_size += field->len();
    }
  }
  
  // ← 断点：查看最终的 record_size
  // 例如：record_size = 4 + 2 + 5 + 2 + 4096 = 4109
  
  // 2. 分配记录缓冲区
  char *record_data = (char *)malloc(record_size);  // ← 断点
  memset(record_data, 0, record_size);
  
  int offset = 0;
  
  // 3. 写入记录大小
  *(int *)(record_data + offset) = record_size;  // ← 断点
  offset += sizeof(int);
  
  // ...
}
```

**调试场景2：写入TEXT字段**
```cpp
// 在 Table::make_record() 的字段写入循环中
for (int i = 0; i < value_num; i++) {
  const FieldMeta *field = table_meta_.field(i + sys_field_num);
  const Value &value = values[i];
  
  if (field->type() == AttrType::TEXT) {  // ← 断点
    // TEXT字段：先写长度，再写数据
    int text_len = value.length();
    
    if (text_len > 4096) {
      text_len = 4096;  // 截断
    }
    
    // ← 断点：写入长度（2字节）
    *(short *)(record_data + offset) = (short)text_len;
    // 查看内存：record_data + offset 的值
    offset += sizeof(short);
    
    // ← 断点：写入数据
    memcpy(record_data + offset, value.data(), text_len);
    // F10单步后，查看内存是否正确写入
    offset += text_len;
    
    // ← 检查 offset 的值，确认位置正确
    
  } else {
    // 定长字段
    memcpy(record_data + offset, value.data(), field->len());
    offset += field->len();
  }
}
```

**调试场景3：插入变长记录**
```cpp
// 在 RecordFileHandler::insert_variable_record() 中
RC RecordFileHandler::insert_variable_record(
    const char *data, 
    int record_size,  // ← 断点：检查记录大小
    RID *rid)
{
  BufferPoolManager &bpm = BufferPoolManager::instance();
  Frame *frame = nullptr;
  
  // 1. 查找有足够空间的页面
  for (PageNum page_num = 1; page_num < file_header_->allocated_pages; page_num++) {
    RC rc = bpm.get_this_page(file_id_, page_num, &frame);  // ← 断点
    if (rc != RC::SUCCESS) {
      continue;
    }
    
    RecordPageHeader *page_header = (RecordPageHeader *)frame->data();
    
    // ← 断点：检查页面空闲空间
    // page_header->free_space_size 是否 >= record_size
    if (page_header->record_capacity - page_header->record_num >= 1 &&
        page_header->free_space_size >= record_size) {  // ← 条件断点
      // 找到合适的页面
      LOG_DEBUG("found page with enough space: %d", page_num);
      break;
    }
    
    bpm.unpin_page(file_id_, page_num);
    frame = nullptr;
  }
  
  // 2. 如果没有合适的页面，分配新页面
  if (frame == nullptr) {  // ← 断点：可能需要分配新页
    LOG_DEBUG("no suitable page found, allocating new page");
    RC rc = allocate_page(&frame);  // ← F11 进入
    if (rc != RC::SUCCESS) {
      return rc;
    }
  }
  
  // 3. 在页面中插入记录
  char *page_data = frame->data();
  RecordPageHeader *page_header = (RecordPageHeader *)page_data;
  
  int offset = page_header->free_space_offset;  // ← 检查插入位置
  
  // 4. 写入记录大小
  *(int *)(page_data + offset) = record_size;  // ← 断点
  offset += sizeof(int);
  
  // 5. 写入实际数据
  memcpy(page_data + offset, data, record_size);  // ← 断点
  
  // 6. 更新页面元数据
  page_header->record_num++;  // ← Watch: 记录数增加
  page_header->free_space_offset += sizeof(int) + record_size;  // ← 偏移量更新
  page_header->free_space_size -= sizeof(int) + record_size;    // ← 剩余空间减少
  
  // ← 断点：检查更新后的页面头信息
  
  // 7. 设置RID
  rid->page_num = frame->page_num();
  rid->slot_num = offset;  // 变长记录使用偏移量
  
  // ← 检查 RID 的值
  
  bpm.mark_dirty(file_id_, frame->page_num());
  bpm.unpin_page(file_id_, frame->page_num());
  
  return RC::SUCCESS;
}
```

**Watch 窗口观察**：
```
record_size                      // 记录总大小
text_len                         // TEXT字段长度
offset                           // 当前写入偏移
page_header->free_space_size     // 页面剩余空间
page_header->record_num          // 页面中的记录数
rid                              // 返回的记录ID
```

**调试技巧 - 测试大文本**：
```python
# 生成测试数据
text_4k = 'A' * 4096      # 正好4096字节
text_5k = 'B' * 5000      # 超过4096，会被截断
text_small = 'C' * 100    # 小文本

# 测试SQL
INSERT INTO articles VALUES (1, 'Test 4K', '{text_4k}');
INSERT INTO articles VALUES (2, 'Test 5K', '{text_5k}');  # 应该被截断
```

**验证截断**：
```cpp
// 在截断后添加日志断点
if (text_len > 4096) {
  text_len = 4096;
  // ← 日志断点：TEXT truncated: original={原长度}, new=4096
}
```

### 💻 代码详解

#### 6.1 变长记录格式

**记录格式设计**：

```
定长字段记录：
[field1(4B)] [field2(4B)] [field3(10B)] ...

变长字段记录（支持TEXT）：
[record_size(4B)] [null_bitmap(NB)] [field1_offset(2B)] [field1_len(2B)] 
[field2_offset(2B)] [field2_len(2B)] ... [field1_data] [field2_data] ...
```

**record_manager.cpp**：

```cpp
// 变长记录的插入
RC RecordFileHandler::insert_variable_record(
    const char *data, 
    int record_size, 
    RID *rid)
{
  // 1. 找到有足够空间的页面
  BufferPoolManager &bpm = BufferPoolManager::instance();
  Frame *frame = nullptr;
  
  // 遍历已有页面，查找空闲空间
  for (PageNum page_num = 1; page_num < file_header_->allocated_pages; page_num++) {
    RC rc = bpm.get_this_page(file_id_, page_num, &frame);
    if (rc != RC::SUCCESS) {
      continue;
    }
    
    // 检查页面是否有足够空间
    RecordPageHeader *page_header = (RecordPageHeader *)frame->data();
    if (page_header->record_capacity - page_header->record_num >= 1 &&
        page_header->free_space_size >= record_size) {
      // 找到合适的页面
      break;
    }
    
    bpm.unpin_page(file_id_, page_num);
    frame = nullptr;
  }

  // 2. 如果没有合适的页面，分配新页面
  if (frame == nullptr) {
    RC rc = allocate_page(&frame);
    if (rc != RC::SUCCESS) {
      return rc;
    }
  }

  // 3. 在页面中插入记录
  char *page_data = frame->data();
  RecordPageHeader *page_header = (RecordPageHeader *)page_data;
  
  // 计算插入位置
  int offset = page_header->free_space_offset;
  
  // 4. 写入记录大小
  *(int *)(page_data + offset) = record_size;
  offset += sizeof(int);
  
  // 5. 写入实际数据
  memcpy(page_data + offset, data, record_size);
  
  // 6. 更新页面元数据
  page_header->record_num++;
  page_header->free_space_offset += sizeof(int) + record_size;
  page_header->free_space_size -= sizeof(int) + record_size;
  
  // 7. 设置RID
  rid->page_num = frame->page_num();
  rid->slot_num = offset;  // 变长记录使用偏移量作为slot_num
  
  // 8. 标记页面为脏页
  bpm.mark_dirty(file_id_, frame->page_num());
  bpm.unpin_page(file_id_, frame->page_num());
  
  return RC::SUCCESS;
}
```

#### 6.2 TEXT字段处理

**table.cpp**：

```cpp
RC Table::make_record(int value_num, const Value *values, Record &record)
{
  // 1. 计算记录总大小
  int record_size = sizeof(int);  // 记录大小字段
  bool has_variable_field = false;
  
  for (int i = 0; i < value_num; i++) {
    const FieldMeta *field = table_meta_.field(i + sys_field_num);
    
    if (field->type() == AttrType::TEXT) {
      has_variable_field = true;
      
      // TEXT字段：大小 = 2字节长度 + 实际数据长度
      const Value &value = values[i];
      int text_len = value.length();
      
      // 截断超过4096字节的数据
      if (text_len > 4096) {
        text_len = 4096;
      }
      
      record_size += sizeof(short) + text_len;
    } else {
      // 定长字段
      record_size += field->len();
    }
  }

  // 2. 分配记录缓冲区
  char *record_data = (char *)malloc(record_size);
  memset(record_data, 0, record_size);
  
  int offset = 0;
  
  // 3. 写入记录大小
  *(int *)(record_data + offset) = record_size;
  offset += sizeof(int);

  // 4. 写入各字段数据
  for (int i = 0; i < value_num; i++) {
    const FieldMeta *field = table_meta_.field(i + sys_field_num);
    const Value &value = values[i];
    
    if (field->type() == AttrType::TEXT) {
      // TEXT字段：先写长度，再写数据
      int text_len = value.length();
      if (text_len > 4096) {
        text_len = 4096;  // 截断
      }
      
      // 写入长度（2字节）
      *(short *)(record_data + offset) = (short)text_len;
      offset += sizeof(short);
      
      // 写入数据
      memcpy(record_data + offset, value.data(), text_len);
      offset += text_len;
      
    } else {
      // 定长字段：直接写入
      memcpy(record_data + offset, value.data(), field->len());
      offset += field->len();
    }
  }

  // 5. 设置记录对象
  record.set_data_owner(record_data, record_size);
  
  return RC::SUCCESS;
}
```

---

## 7. null - NULL值支持

### 📝 功能概述

支持字段为NULL：
- 字段属性：NULLABLE / NOT NULL
- NULL值比较：NULL与任何值比较都是FALSE
- 聚合函数：跳过NULL值

### 🗂️ 关键文件

```
Parser:   yacc_sql.y (NULLABLE关键字)
Meta:     field_meta.cpp (nullable标志)
Storage:  value.cpp (NULL值表示)
Execute:  各种算子 (NULL值处理)
```

### 🐛 调试指南 - NULL 值支持

#### 断点位置设置

**调试 NULL 值的关键断点**：

1. **插入验证**：`src/observer/sql/stmt/insert_stmt.cpp`
   ```
   断点1：类型检查循环
   断点2：values[i].is_null() 判断
   断点3：field_meta->nullable() 检查
   断点4：返回 RC::NULL_CANT_INSERT 的位置
   ```

2. **NULL值存储**：`src/observer/storage/table/table.cpp`
   ```
   断点1：make_record_with_null() 函数开始
   断点2：计算 null_bitmap_size
   断点3：设置NULL位图的循环
   断点4：跳过NULL字段的逻辑
   ```

3. **NULL值比较**：`src/observer/common/value.cpp`
   ```
   断点1：Value::compare() 函数
   断点2：is_null() 判断分支
   断点3：返回特殊值 INT_MIN
   ```

4. **查询过滤**：`src/observer/sql/operator/physical_operator/predicate_physical_operator.cpp`
   ```
   断点1：predicate_->get_value() 调用
   断点2：value.is_null() 检查
   断点3：跳过NULL行的 continue 语句
   ```

5. **聚合函数**：`src/observer/sql/expr/aggregator.cpp`
   ```
   断点：各个聚合器的 accumulate() 中的 is_null() 检查
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE TABLE users(id int, name char(20) nullable, age int);
INSERT INTO users VALUES (1, 'Alice', 25);
INSERT INTO users VALUES (2, NULL, 30);      -- name为NULL
INSERT INTO users VALUES (3, 'Bob', NULL);   -- age为NULL
INSERT INTO users VALUES (4, NULL, NULL);    -- 多个NULL
SELECT * FROM users WHERE name IS NOT NULL;
SELECT COUNT(*), COUNT(name), COUNT(age) FROM users;
```

**调试场景1：插入NULL值验证**
```cpp
// 在 InsertStmt::create() 中
for (auto &values : inserts.tuples) {
  // 检查每个字段的类型
  for (int i = 0; i < field_num; i++) {  // ← 断点
    const FieldMeta *field_meta = table_meta.field(i + sys_field_num);
    const AttrType field_type = field_meta->type();
    const AttrType value_type = values[i].attr_type();
    
    // 类型不匹配，尝试转换
    if (value_type != field_type) {
      Value real_value;
      
      // ← 断点：特殊处理NULL值
      if (values[i].is_null()) {  // ← F10，检查是否为NULL
        // 查看 values[i] 的内容
        
        if (not field_meta->nullable()) {  // ← 断点
          // 字段不允许NULL
          LOG_WARN("field %s does not allow NULL", field_meta->name());
          return RC::NULL_CANT_INSERT;  // ← 返回错误
        }
        // 字段允许NULL，继续
      } else {
        // 类型转换
        RC rc = Value::cast_to(values[i], field_meta->type(), real_value);
        if (OB_FAIL(rc)) {
          return rc;
        }
      }
    }
  }
}
```

**调试场景2：NULL值的存储（NULL位图）**
```cpp
// 在 Table::make_record_with_null() 中
RC Table::make_record_with_null(int value_num, const Value *values, Record &record)
{
  // 1. 计算NULL位图大小
  int null_bitmap_size = (value_num + 7) / 8;  // ← 断点
  // 例如：8个字段 → 1字节，9个字段 → 2字节
  // Watch: null_bitmap_size
  
  int record_size = sizeof(int) + null_bitmap_size;
  
  // 计算实际数据大小（跳过NULL字段）
  for (int i = 0; i < value_num; i++) {  // ← 断点
    if (!values[i].is_null()) {  // ← 检查每个值
      const FieldMeta *field = table_meta_.field(i + sys_field_num);
      record_size += field->len();
    } else {
      // NULL字段不占用数据空间
      LOG_DEBUG("field %d is NULL, skipping data storage", i);
    }
  }
  
  // ← 断点：查看最终 record_size
  
  char *record_data = (char *)malloc(record_size);
  memset(record_data, 0, record_size);
  
  int offset = sizeof(int);
  
  // 设置NULL位图
  char *null_bitmap = record_data + offset;  // ← 断点
  offset += null_bitmap_size;
  
  // ← 断点：开始设置位图
  for (int i = 0; i < value_num; i++) {
    if (values[i].is_null()) {  // ← 断点
      // 设置对应的bit为1
      int byte_index = i / 8;      // 第几个字节
      int bit_index = i % 8;       // 字节内的第几位
      
      null_bitmap[byte_index] |= (1 << bit_index);  // ← 断点
      // 查看 null_bitmap[byte_index] 的值
      // 例如：第1个字段NULL → null_bitmap[0] = 0x01
      //       第2个字段NULL → null_bitmap[0] = 0x03
      
      LOG_DEBUG("set NULL bit for field %d: byte=%d, bit=%d", 
                i, byte_index, bit_index);
      
    } else {
      // 写入实际数据
      const FieldMeta *field = table_meta_.field(i + sys_field_num);
      memcpy(record_data + offset, values[i].data(), field->len());
      offset += field->len();
    }
  }
  
  *(int *)record_data = record_size;
  record.set_data_owner(record_data, record_size);
  
  return RC::SUCCESS;
}
```

**调试场景3：NULL值比较**
```cpp
// 在 Value::compare() 中
int Value::compare(const Value &other) const
{
  // ← 断点：检查是否有NULL
  if (this->is_null() || other.is_null()) {  // ← F10
    // 查看 this 和 other 的状态
    
    LOG_DEBUG("comparing with NULL: this=%s, other=%s",
              this->to_string().c_str(), other->to_string().c_str());
    
    return INT_MIN;  // ← 特殊值，表示比较结果为UNKNOWN
  }
  
  // 正常比较
  // ...
}
```

**调试场景4：WHERE 条件中的NULL处理**
```cpp
// 在 PredicatePhysicalOperator::next() 中
RC PredicatePhysicalOperator::next()
{
  RC rc = RC::SUCCESS;
  
  while (RC::SUCCESS == (rc = children_[0]->next())) {
    Tuple *tuple = children_[0]->current_tuple();
    
    // 计算谓词表达式
    Value value;
    rc = predicate_->get_value(*tuple, value);  // ← 断点
    
    // ← 断点：NULL值处理
    if (value.is_null()) {  // ← F10，检查是否为NULL
      // NULL的比较结果总是UNKNOWN，视为FALSE
      LOG_DEBUG("predicate result is NULL, skipping row");
      continue;  // ← 跳过这一行
    }
    
    // 正常的布尔值判断
    if (value.get_boolean()) {  // ← 断点
      return RC::SUCCESS;  // 满足条件
    }
  }
  
  return rc;
}
```

**调试场景5：聚合函数中的NULL**
```cpp
// 在 CountAggregator::accumulate() 中
void CountAggregator::accumulate(const Value &value)
{
  if (field_name_ == "*") {  // ← 断点
    // COUNT(*) 计数所有行
    count_++;
    LOG_DEBUG("COUNT(*): count=%d", count_);
    return;
  }
  
  // ← 断点：COUNT(field) 只计数非NULL值
  if (!value.is_null()) {  // ← F10，检查是否为NULL
    count_++;
    LOG_DEBUG("COUNT(field): counting non-NULL, count=%d", count_);
  } else {
    LOG_DEBUG("COUNT(field): skipping NULL value");
  }
}

// 在 SumAggregator::accumulate() 中
void SumAggregator::accumulate(const Value &value)
{
  // ← 断点：跳过NULL值
  if (value.is_null()) {  // ← F10
    LOG_DEBUG("SUM: skipping NULL value");
    return;  // ← 关键：忽略NULL
  }
  
  // 正常累加
  if (first_) {
    value_ = value;
    first_ = false;
  } else {
    value_.add(value);
  }
  LOG_DEBUG("SUM: accumulated value=%s", value_.to_string().c_str());
}
```

**Watch 窗口观察**：
```
values[i].is_null()              // 当前值是否为NULL
field_meta->nullable()           // 字段是否允许NULL
null_bitmap_size                 // NULL位图大小
null_bitmap[0]                   // 位图内容
byte_index, bit_index            // 位索引
value.attr_type_                 // 值的类型（NULLS表示NULL）
count_                           // 聚合计数（观察NULL如何影响）
```

**调试技巧 - 可视化NULL位图**：
```cpp
// 在调试器的Watch窗口中添加表达式
// 查看位图的二进制表示
*((unsigned char*)null_bitmap)@null_bitmap_size

// 或者添加辅助函数
void print_null_bitmap(const char *bitmap, int field_count) {
  for (int i = 0; i < field_count; i++) {
    int byte_idx = i / 8;
    int bit_idx = i % 8;
    bool is_null = (bitmap[byte_idx] & (1 << bit_idx)) != 0;
    printf("Field %d: %s\n", i, is_null ? "NULL" : "NOT NULL");
  }
}
```

**测试用例**：
```sql
-- 测试1：插入NULL到NOT NULL字段（应该失败）
CREATE TABLE t1(id int, name char(10));  -- 默认NOT NULL
INSERT INTO t1 VALUES (1, NULL);  -- 失败

-- 测试2：插入NULL到NULLABLE字段（应该成功）
CREATE TABLE t2(id int, name char(10) nullable);
INSERT INTO t2 VALUES (1, NULL);  -- 成功

-- 测试3：NULL值比较
SELECT * FROM t2 WHERE name = NULL;     -- 返回空（错误用法）
SELECT * FROM t2 WHERE name IS NULL;    -- 正确返回NULL行

-- 测试4：聚合函数
SELECT COUNT(*), COUNT(name) FROM t2;   -- 观察差异
```

### 💻 代码详解

#### 7.1 语法解析

**yacc_sql.y**：

```yacc
nullable:
    /* empty */ {
        $$ = false;  // 默认NOT NULL
    }
    | NULLABLE {
        $$ = true;   // 允许NULL
    }
    | NOT NULL_T {
        $$ = false;  // 明确NOT NULL
    }
    ;

attr_def:
    ID type nullable {
        $$ = new AttrInfoSqlNode;
        $$->type = (AttrType)$2;
        $$->name = $1;
        $$->length = 4;
        $$->nullable = $3;  // ← 设置nullable属性
        free($1);
    }
    ;
```

#### 7.2 NULL值的存储

**value.cpp**：

```cpp
class Value
{
public:
  // NULL值用特殊的内部状态表示
  void set_null()
  {
    attr_type_ = AttrType::NULLS;  // 特殊类型
    // 不分配实际数据
  }

  bool is_null() const
  {
    return attr_type_ == AttrType::NULLS;
  }

  // NULL值比较规则
  int compare(const Value &other) const
  {
    // NULL与任何值比较都返回false（通过特殊返回值表示）
    if (this->is_null() || other.is_null()) {
      return INT_MIN;  // 特殊值，表示比较结果为UNKNOWN
    }
    
    // 正常比较
    // ...
  }
};
```

**记录存储（使用NULL位图）**：

```cpp
// 记录格式（支持NULL）：
// [record_header] [null_bitmap] [field_data...]

// null_bitmap: 每个bit表示一个字段是否为NULL
// 例如：8个字段需要1个字节的bitmap

RC Table::make_record_with_null(int value_num, const Value *values, Record &record)
{
  int null_bitmap_size = (value_num + 7) / 8;  // 向上取整
  int record_size = sizeof(int) + null_bitmap_size;
  
  // 计算实际数据大小
  for (int i = 0; i < value_num; i++) {
    if (!values[i].is_null()) {
      const FieldMeta *field = table_meta_.field(i + sys_field_num);
      record_size += field->len();
    }
  }
  
  char *record_data = (char *)malloc(record_size);
  memset(record_data, 0, record_size);
  
  int offset = sizeof(int);
  
  // 设置NULL位图
  char *null_bitmap = record_data + offset;
  offset += null_bitmap_size;
  
  for (int i = 0; i < value_num; i++) {
    if (values[i].is_null()) {
      // 设置对应的bit为1
      int byte_index = i / 8;
      int bit_index = i % 8;
      null_bitmap[byte_index] |= (1 << bit_index);
    } else {
      // 写入实际数据
      const FieldMeta *field = table_meta_.field(i + sys_field_num);
      memcpy(record_data + offset, values[i].data(), field->len());
      offset += field->len();
    }
  }
  
  *(int *)record_data = record_size;
  record.set_data_owner(record_data, record_size);
  
  return RC::SUCCESS;
}
```

#### 7.3 NULL值的查询处理

**predicate_physical_operator.cpp**：

```cpp
RC PredicatePhysicalOperator::next()
{
  RC rc = RC::SUCCESS;
  
  // 逐行扫描
  while (RC::SUCCESS == (rc = children_[0]->next())) {
    Tuple *tuple = children_[0]->current_tuple();
    
    // 计算谓词表达式
    Value value;
    rc = predicate_->get_value(*tuple, value);
    
    // NULL值处理
    if (value.is_null()) {
      // NULL的比较结果总是UNKNOWN，视为FALSE
      continue;  // 跳过这一行
    }
    
    // 正常的布尔值判断
    if (value.get_boolean()) {
      return RC::SUCCESS;  // 满足条件
    }
  }
  
  return rc;
}
```

#### 7.4 聚合函数中的NULL处理

**aggregator.cpp**：

```cpp
class SumAggregator : public Aggregator
{
public:
  void accumulate(const Value &value) override
  {
    // 跳过NULL值
    if (value.is_null()) {
      return;  // ← 关键：忽略NULL
    }
    
    // 正常累加
    if (first_) {
      value_ = value;
      first_ = false;
    } else {
      value_.add(value);
    }
  }
};

class CountAggregator : public Aggregator
{
public:
  void accumulate(const Value &value) override
  {
    // COUNT(*) 计数所有行
    if (field_name_ == "*") {
      count_++;
      return;
    }
    
    // COUNT(field) 只计数非NULL值
    if (!value.is_null()) {
      count_++;  // ← 关键：只计数非NULL
    }
  }
};

class AvgAggregator : public Aggregator
{
public:
  void accumulate(const Value &value) override
  {
    // 跳过NULL值
    if (value.is_null()) {
      return;
    }
    
    sum_.add(value);
    count_++;
  }
  
  Value evaluate() override
  {
    if (count_ == 0) {
      return Value::null_value();  // 没有有效值，返回NULL
    }
    return sum_.divide(count_);
  }
};
```

---

## 8. multi-index - 多列索引

### 📝 功能概述

支持多列组合索引：
```sql
CREATE INDEX idx_name ON t1(col1, col2, col3);
```

**特点**：
- 前缀匹配原则
- 排序规则：先按col1，再按col2，再按col3
- 查询必须使用前缀才能用上索引

### 🗂️ 关键文件

```
Meta:    index_meta.cpp (多字段索引元数据)
Index:   bplus_tree_index.cpp (多列键的比较)
```

### 🐛 调试指南 - 多列索引

#### 断点位置设置

**调试多列索引的关键断点**：

1. **索引元数据**：`src/observer/storage/index/index_meta.cpp`
   ```
   断点1：IndexMeta::init() - 保存多个字段
   断点2：key_length() - 计算总键长度
   ```

2. **构造组合键**：`src/observer/storage/index/bplus_tree_index.cpp`
   ```
   断点1：insert_entry() - 提取多个字段值
   断点2：构造键的循环
   断点3：memcpy() 复制每个字段
   ```

3. **键比较**：`src/observer/storage/index/bplus_tree.cpp`
   ```
   断点1：compare_key() 函数开始
   断点2：比较每个字段的循环
   断点3：cmp != 0 时的返回
   ```

4. **索引选择**：`src/observer/sql/optimizer/physical_plan_generator.cpp`
   ```
   断点1：try_to_use_index() - 检查前缀匹配
   断点2：matched_fields 计数
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE TABLE orders(order_id int, user_id int, product_id int, quantity int);
CREATE INDEX idx_order ON orders(user_id, product_id);
INSERT INTO orders VALUES (1, 100, 1001, 5);
INSERT INTO orders VALUES (2, 100, 1002, 3);
INSERT INTO orders VALUES (3, 101, 1001, 2);

-- 能用上索引（前缀匹配）
SELECT * FROM orders WHERE user_id = 100;
SELECT * FROM orders WHERE user_id = 100 AND product_id = 1001;

-- 不能用上索引（跳过了user_id）
SELECT * FROM orders WHERE product_id = 1001;
```

**调试场景1：创建多列索引**
```cpp
// 在 IndexMeta::init() 中
RC IndexMeta::init(const char *name, 
                   const std::vector<const FieldMeta *> &fields,
                   bool unique)
{
  name_ = name;  // ← "idx_order"
  unique_ = unique;
  
  // ← 断点：保存多个字段
  for (const FieldMeta *field : fields) {  // ← 循环
    fields_.push_back(field->name());
    // 第1次：fields_ = ["user_id"]
    // 第2次：fields_ = ["user_id", "product_id"]
    LOG_DEBUG("added field to index: %s", field->name());
  }
  
  // ← 断点：检查 fields_ 的最终内容
  // fields_.size() = 2
  // fields_[0] = "user_id"
  // fields_[1] = "product_id"
  
  return RC::SUCCESS;
}

// 计算键长度
int IndexMeta::key_length() const
{
  int total_len = 0;
  
  // ← 断点
  for (const std::string &field_name : fields_) {
    const FieldMeta *field = table_meta_->field(field_name.c_str());
    total_len += field->len();
    // user_id (int): 4字节
    // product_id (int): 4字节
    // 总计：8字节
  }
  
  // ← 断点：total_len = 8
  return total_len;
}
```

**调试场景2：构造组合键**
```cpp
// 在 BplusTreeIndex::insert_entry() 中
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
  // 1. 分配键缓冲区
  char *key = new char[index_meta_.key_length()];  // ← 断点：8字节
  int offset = 0;
  
  // ← 断点：遍历所有字段
  for (const std::string &field_name : index_meta_.fields()) {
    const FieldMeta *field_meta = table_->table_meta().field(field_name.c_str());
    
    // 从记录中获取字段值
    const char *field_data = record + field_meta->offset();  // ← 断点
    int field_len = field_meta->len();
    
    // 假设记录：order_id=1, user_id=100, product_id=1001, quantity=5
    // 第1次循环：field_name="user_id", field_data指向100, field_len=4
    // 第2次循环：field_name="product_id", field_data指向1001, field_len=4
    
    // ← 断点：复制到键缓冲区
    memcpy(key + offset, field_data, field_len);
    // key的内容：[100的4字节] [1001的4字节]
    
    offset += field_len;
    // 第1次后：offset = 4
    // 第2次后：offset = 8
  }
  
  // ← 断点：查看完整的key
  // 在内存视图中查看 key 的8字节内容
  // 应该是：[0x64, 0x00, 0x00, 0x00, 0xE9, 0x03, 0x00, 0x00]
  //         (100的小端表示)     (1001的小端表示)
  
  // 2. 插入到B+树
  RC rc = index_handler_.insert_entry(key, rid);  // ← F11进入
  
  delete[] key;
  return rc;
}
```

**调试场景3：多列键比较**
```cpp
// 在 BplusTree::compare_key() 中
int BplusTree::compare_key(const char *key1, const char *key2)
{
  int offset = 0;
  
  // ← 断点：依次比较每个字段
  for (const FieldMeta *field_meta : field_metas_) {  // ← 循环
    int cmp = 0;
    
    // 根据字段类型进行比较
    switch (field_meta->type()) {
      case AttrType::INTS: {  // ← 断点
        int v1 = *(int *)(key1 + offset);
        int v2 = *(int *)(key2 + offset);
        
        // 比较 user_id
        // 假设：key1=(100, 1001), key2=(100, 1002)
        // v1 = 100, v2 = 100
        
        cmp = v1 - v2;  // ← cmp = 0 (相等)
        
        LOG_DEBUG("comparing field %s: %d vs %d, cmp=%d",
                  field_meta->name(), v1, v2, cmp);
      } break;
      
      // 其他类型...
    }
    
    // ← 断点：如果当前字段不相等，直接返回
    if (cmp != 0) {  // ← F10
      LOG_DEBUG("fields differ at %s, returning %d", 
                field_meta->name(), cmp);
      return cmp;  // ← 前面字段优先
    }
    
    // 当前字段相等，继续比较下一个字段
    offset += field_meta->len();
    // 第1次后：offset = 4 (准备比较 product_id)
  }
  
  // ← 断点：所有字段都相等
  LOG_DEBUG("all fields equal");
  return 0;
}
```

**调试场景4：前缀匹配查询优化**
```cpp
// 在 PhysicalPlanGenerator::try_to_use_index() 中
RC PhysicalPlanGenerator::try_to_use_index(...)
{
  // 遍历所有索引
  for (Index *idx : table->indexes()) {  // ← 断点
    const std::vector<std::string> &index_fields = idx->index_meta().fields();
    
    // 索引字段：["user_id", "product_id"]
    
    // ← 断点：检查查询条件是否匹配索引前缀
    bool can_use = true;
    int matched_fields = 0;
    
    for (const std::string &field_name : index_fields) {  // ← 循环
      // 查找查询条件中是否有这个字段
      bool found = false;
      
      for (FilterUnit *unit : filter_units) {  // ← 断点
        if (unit->field_name() == field_name) {
          found = true;
          matched_fields++;
          LOG_DEBUG("matched index field: %s", field_name.c_str());
          break;
        }
      }
      
      if (!found) {
        // 前缀中断了
        LOG_DEBUG("prefix broken at field: %s", field_name.c_str());
        break;  // ← 断点
      }
    }
    
    // ← 断点：检查匹配结果
    // 查询：WHERE user_id = 100 AND product_id = 1001
    // matched_fields = 2 → 可以使用索引
    
    // 查询：WHERE product_id = 1001
    // matched_fields = 0 → 不能使用索引（跳过了user_id）
    
    if (matched_fields > 0) {  // ← 断点
      index = idx;
      LOG_INFO("using index %s with %d matched fields", 
               idx->index_meta().name(), matched_fields);
      return RC::SUCCESS;
    }
  }
  
  return RC::NOT_FOUND;
}
```

**Watch 窗口观察**：
```
fields_.size()               // 索引字段数量
index_meta_.key_length()     // 键总长度
offset                       // 当前构造/比较偏移
key                          // 组合键内容
matched_fields               // 匹配的字段数
cmp                          // 比较结果
```

**调试技巧 - 可视化组合键**：
```cpp
// 辅助函数：打印组合键
std::string key_to_string(const char *key) {
  std::stringstream ss;
  ss << "(";
  int offset = 0;
  for (const FieldMeta *field : field_metas_) {
    if (offset > 0) ss << ", ";
    
    switch (field->type()) {
      case AttrType::INTS:
        ss << *(int*)(key + offset);
        break;
      case AttrType::CHARS:
        ss << std::string(key + offset, field->len());
        break;
    }
    offset += field->len();
  }
  ss << ")";
  return ss.str();
}

// 在比较时打印
LOG_DEBUG("comparing keys: %s vs %s", 
          key_to_string(key1).c_str(), 
          key_to_string(key2).c_str());
```

**测试用例**：
```sql
-- 测试前缀匹配
-- 能用索引
EXPLAIN SELECT * FROM orders WHERE user_id = 100;
EXPLAIN SELECT * FROM orders WHERE user_id = 100 AND product_id = 1001;

-- 不能用索引
EXPLAIN SELECT * FROM orders WHERE product_id = 1001;

-- 测试排序
SELECT * FROM orders ORDER BY user_id, product_id;  -- 索引顺序
```

### 💻 代码详解

#### 8.1 索引元数据

**index_meta.cpp**：

```cpp
class IndexMeta
{
public:
  // 初始化多列索引
  RC init(const char *name, 
          const std::vector<const FieldMeta *> &fields,
          bool unique)
  {
    name_ = name;
    unique_ = unique;
    
    // 保存多个字段
    for (const FieldMeta *field : fields) {
      fields_.push_back(field->name());
    }
    
    return RC::SUCCESS;
  }

  // 获取所有字段
  const std::vector<std::string> &fields() const { return fields_; }
  
  // 计算索引键的总长度
  int key_length() const
  {
    int total_len = 0;
    for (const std::string &field_name : fields_) {
      const FieldMeta *field = table_meta_->field(field_name.c_str());
      total_len += field->len();
    }
    return total_len;
  }

private:
  std::string              name_;     // 索引名
  std::vector<std::string> fields_;   // ← 多个字段名
  bool                     unique_;   // 是否唯一索引
};
```

#### 8.2 组合键的构造

**bplus_tree_index.cpp**：

```cpp
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
  // 1. 从记录中提取多个字段的值，组合成键
  char *key = new char[index_meta_.key_length()];
  int offset = 0;
  
  for (const std::string &field_name : index_meta_.fields()) {
    const FieldMeta *field_meta = table_->table_meta().field(field_name.c_str());
    
    // 从记录中获取字段值
    const char *field_data = record + field_meta->offset();
    int field_len = field_meta->len();
    
    // 复制到键缓冲区
    memcpy(key + offset, field_data, field_len);
    offset += field_len;
  }
  
  // 2. 插入到B+树
  RC rc = index_handler_.insert_entry(key, rid);
  
  delete[] key;
  return rc;
}
```

#### 8.3 多列键的比较

**bplus_tree.cpp**：

```cpp
int BplusTree::compare_key(const char *key1, const char *key2)
{
  int offset = 0;
  
  // 依次比较每个字段
  for (const FieldMeta *field_meta : field_metas_) {
    int cmp = 0;
    
    // 根据字段类型进行比较
    switch (field_meta->type()) {
      case AttrType::INTS: {
        int v1 = *(int *)(key1 + offset);
        int v2 = *(int *)(key2 + offset);
        cmp = v1 - v2;
      } break;
      
      case AttrType::CHARS: {
        cmp = strncmp(key1 + offset, key2 + offset, field_meta->len());
      } break;
      
      case AttrType::FLOATS: {
        float v1 = *(float *)(key1 + offset);
        float v2 = *(float *)(key2 + offset);
        cmp = (v1 > v2) ? 1 : ((v1 < v2) ? -1 : 0);
      } break;
      
      case AttrType::DATE: {
        int v1 = *(int *)(key1 + offset);
        int v2 = *(int *)(key2 + offset);
        cmp = v1 - v2;
      } break;
    }
    
    // 如果当前字段不相等，直接返回比较结果
    if (cmp != 0) {
      return cmp;  // ← 前面字段优先
    }
    
    // 当前字段相等，继续比较下一个字段
    offset += field_meta->len();
  }
  
  // 所有字段都相等
  return 0;
}
```

#### 8.4 前缀匹配查询

**查询优化器选择索引**：

```cpp
RC PhysicalPlanGenerator::try_to_use_index(
    Table *table,
    FilterUnit *filter_unit,
    Index *&index)
{
  // 遍历所有索引
  for (Index *idx : table->indexes()) {
    const std::vector<std::string> &index_fields = idx->index_meta().fields();
    
    // 检查查询条件是否匹配索引前缀
    // 例如：索引是(col1, col2, col3)
    //      查询条件有col1 = ? AND col2 = ?
    //      可以使用索引（前缀匹配）
    
    bool can_use = true;
    int matched_fields = 0;
    
    for (const std::string &field_name : index_fields) {
      // 查找查询条件中是否有这个字段
      bool found = false;
      for (FilterUnit *unit : filter_units) {
        if (unit->field_name() == field_name) {
          found = true;
          matched_fields++;
          break;
        }
      }
      
      if (!found) {
        // 前缀中断了
        break;
      }
    }
    
    // 如果匹配了索引的前缀，可以使用
    if (matched_fields > 0) {
      index = idx;
      return RC::SUCCESS;
    }
  }
  
  return RC::NOT_FOUND;
}
```

---

## 9. unique - 唯一索引

### 📝 功能概述

唯一索引约束：同一个键值只能出现一次
```sql
CREATE UNIQUE INDEX idx_email ON users(email);
INSERT INTO users VALUES (1, 'test@test.com');  -- OK
INSERT INTO users VALUES (2, 'test@test.com');  -- FAILURE: 重复键
```

### 🗂️ 关键文件

```
Meta:  index_meta.cpp (unique标志)
Index: bplus_tree_index.cpp (重复键检查)
```

### 🐛 调试指南 - 唯一索引

#### 断点位置设置

**调试唯一索引的关键断点**：

1. **创建索引**：`src/observer/storage/table/table.cpp`
   ```
   断点：create_index() 中传递 unique 参数
   ```

2. **索引插入**：`src/observer/storage/index/bplus_tree_index.cpp`
   ```
   断点1：insert_entry() 函数开始
   断点2：index_meta_.unique() 判断
   断点3：scan_equal() 检查重复
   断点4：返回 RC::DUPLICATE_KEY
   ```

3. **记录插入回滚**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::insert_record() 中的索引更新循环
   断点2：rc != RC::SUCCESS 的分支
   断点3：record_handler_->delete_record() 回滚
   断点4：已更新索引的回滚循环
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE TABLE users(id int, email char(50));
CREATE UNIQUE INDEX idx_email ON users(email);

INSERT INTO users VALUES (1, 'alice@test.com');  -- 成功
INSERT INTO users VALUES (2, 'bob@test.com');    -- 成功
INSERT INTO users VALUES (3, 'alice@test.com');  -- 失败：重复
```

**调试场景1：唯一索引插入检查**
```cpp
// 在 BplusTreeIndex::insert_entry() 中
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
  // 1. 构造键
  char *key = make_key(record);  // ← 断点
  // 例如：key = "alice@test.com"
  
  // ← 断点：如果是唯一索引，先检查键是否已存在
  if (index_meta_.unique()) {  // ← F10，检查是否为唯一索引
    LOG_DEBUG("checking uniqueness for key: %s", key_to_string(key).c_str());
    
    std::vector<RID> rids;
    
    // 在索引中查找相同的键
    RC rc = index_handler_.scan_equal(key, key, rids);  // ← F11进入
    // scan_equal 会在B+树中查找与key相等的所有记录
    
    // ← 断点：检查查找结果
    if (rc == RC::SUCCESS && !rids.empty()) {  // ← F10
      // 键已存在！
      LOG_WARN("duplicate key in unique index. key=%s, existing_rids=%zu", 
               key_to_string(key).c_str(), rids.size());
      
      // 查看已存在的记录
      for (const RID &existing_rid : rids) {
        LOG_DEBUG("existing record at page=%d, slot=%d",
                  existing_rid.page_num, existing_rid.slot_num);
      }
      
      delete[] key;
      return RC::DUPLICATE_KEY;  // ← 返回重复键错误
    }
    
    LOG_DEBUG("key is unique, proceeding with insert");
  }
  
  // 3. 键不存在（或非唯一索引），插入索引
  RC rc = index_handler_.insert_entry(key, rid);  // ← 断点
  
  delete[] key;
  return rc;
}
```

**调试场景2：插入记录时的索引冲突处理**
```cpp
// 在 Table::insert_record() 中
RC Table::insert_record(Record &record)
{
  // 1. 先插入记录到数据文件
  RC rc = record_handler_->insert_record(
      record.data(), 
      record.len(), 
      &record.rid()
  );  // ← 断点：记录插入成功
  
  if (rc != RC::SUCCESS) {
    return rc;
  }
  
  LOG_DEBUG("record inserted at page=%d, slot=%d", 
            record.rid().page_num, record.rid().slot_num);
  
  // 2. 更新所有索引
  for (Index *index : indexes_) {  // ← 断点：遍历所有索引
    LOG_DEBUG("updating index: %s", index->index_meta().name());
    
    rc = index->insert_entry(record.data(), &record.rid());  // ← F11进入
    
    // ← 关键断点：索引插入可能失败（唯一键冲突）
    if (rc != RC::SUCCESS) {  // ← F10
      LOG_ERROR("failed to insert into index %s: %s", 
                index->index_meta().name(), strrc(rc));
      
      // ← 断点：开始回滚
      
      // 步骤1：删除已插入的记录
      LOG_DEBUG("rolling back: deleting record");
      record_handler_->delete_record(&record.rid());  // ← F10
      
      // 步骤2：删除已更新的索引
      LOG_DEBUG("rolling back: removing from previously updated indexes");
      for (Index *idx : indexes_) {
        if (idx == index) {
          break;  // 当前索引还没插入成功，跳过
        }
        
        // 从已更新的索引中删除
        LOG_DEBUG("removing from index: %s", idx->index_meta().name());
        idx->delete_entry(record.data(), &record.rid());  // ← F10
      }
      
      return rc;  // ← 返回错误（RC::DUPLICATE_KEY）
    }
    
    LOG_DEBUG("index %s updated successfully", index->index_meta().name());
  }
  
  // 所有索引更新成功
  return RC::SUCCESS;
}
```

**调试场景3：测试唯一索引冲突**
```cpp
// 设置条件断点
// 在 BplusTreeIndex::insert_entry() 的 unique 检查处
// 条件：strcmp(key, "alice@test.com") == 0

// 第1次插入：alice@test.com
// - scan_equal() 返回空
// - 插入成功

// 第2次插入：alice@test.com
// - scan_equal() 返回 1 个 RID  ← 断点会停在这里
// - 返回 RC::DUPLICATE_KEY
// - Table::insert_record() 捕获错误并回滚
```

**Watch 窗口观察**：
```
index_meta_.unique()         // 是否唯一索引
key                          // 要插入的键值
rids.size()                  // 查找到的重复记录数
rc                           // 返回码
indexes_.size()              // 表的索引数量
index                        // 当前正在更新的索引
```

**调试技巧 - 追踪回滚过程**：
```cpp
// 添加日志断点追踪完整流程

// 在 insert_record() 入口
LOG_INFO("=== START INSERT RECORD ===");

// 在每个索引更新前
LOG_INFO("updating index %d/%zu: %s", 
         index_num, indexes_.size(), index->index_meta().name());

// 在回滚开始
LOG_INFO("=== START ROLLBACK ===");

// 在回滚的每个步骤
LOG_INFO("rollback step: %s", step_description);

// 在返回错误
LOG_INFO("=== INSERT FAILED: %s ===", strrc(rc));
```

**测试复杂场景**：
```sql
-- 场景1：多个索引，其中一个唯一索引冲突
CREATE TABLE users(id int, email char(50), phone char(20));
CREATE INDEX idx_id ON users(id);
CREATE UNIQUE INDEX idx_email ON users(email);
CREATE INDEX idx_phone ON users(phone);

INSERT INTO users VALUES (1, 'alice@test.com', '1234567890');  -- 成功
INSERT INTO users VALUES (2, 'alice@test.com', '0987654321');  -- 失败

-- 调试：观察3个索引的更新顺序和回滚过程

-- 场景2：组合唯一索引
CREATE TABLE orders(user_id int, product_id int, quantity int);
CREATE UNIQUE INDEX idx_order ON orders(user_id, product_id);

INSERT INTO orders VALUES (100, 1001, 5);  -- 成功
INSERT INTO orders VALUES (100, 1001, 3);  -- 失败：(100, 1001)重复
INSERT INTO orders VALUES (100, 1002, 3);  -- 成功：(100, 1002)不重复
```

**验证回滚完整性**：
```cpp
// 在测试后检查
// 1. 数据文件中不应有失败的记录
// 2. 所有索引应该一致
// 3. 没有部分更新的索引

// 添加断言
ASSERT(record_count == index_entry_count, 
       "data and index inconsistent");
```

### 💻 代码详解

**bplus_tree_index.cpp**：

```cpp
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
  // 1. 构造键
  char *key = make_key(record);
  
  // 2. 如果是唯一索引，先检查键是否已存在
  if (index_meta_.unique()) {
    std::vector<RID> rids;
    RC rc = index_handler_.scan_equal(key, key, rids);
    
    if (rc == RC::SUCCESS && !rids.empty()) {
      // 键已存在
      LOG_WARN("duplicate key in unique index. key=%s", key_to_string(key).c_str());
      delete[] key;
      return RC::DUPLICATE_KEY;  // ← 返回重复键错误
    }
  }
  
  // 3. 插入索引
  RC rc = index_handler_.insert_entry(key, rid);
  
  delete[] key;
  return rc;
}
```

**插入记录时的检查（table.cpp）**：

```cpp
RC Table::insert_record(Record &record)
{
  // 1. 先插入记录到数据文件
  RC rc = record_handler_->insert_record(
      record.data(), 
      record.len(), 
      &record.rid()
  );
  
  if (rc != RC::SUCCESS) {
    return rc;
  }

  // 2. 更新所有索引
  for (Index *index : indexes_) {
    rc = index->insert_entry(record.data(), &record.rid());
    
    if (rc != RC::SUCCESS) {
      // 索引插入失败（可能是唯一键冲突）
      
      // 回滚：删除已插入的记录
      record_handler_->delete_record(&record.rid());
      
      // 回滚：删除已更新的索引
      for (Index *idx : indexes_) {
        if (idx == index) {
          break;  // 当前索引还没插入成功
        }
        idx->delete_entry(record.data(), &record.rid());
      }
      
      return rc;  // ← 返回错误（RC::DUPLICATE_KEY）
    }
  }

  return RC::SUCCESS;
}
```

---

由于篇幅，我将创建第三部分继续讲解剩余功能。让我继续：

