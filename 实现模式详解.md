# 🎯 MiniOB 功能实现通用模式和调试方案

## 📋 目录

- [通用实现模式](#通用实现模式)
- [五阶段开发流程](#五阶段开发流程)
- [各功能实现分析](#各功能实现分析)
- [系统化调试方案](#系统化调试方案)
- [开发思路总结](#开发思路总结)

---

## 🔄 通用实现模式

基于对 MiniOB 代码的分析，所有 SQL 功能都遵循**统一的五阶段处理流程**：

```
用户输入 SQL 字符串
       ↓
┌──────────────────────────┐
│  1. Parser 解析阶段       │ → ParsedSqlNode
│  (词法 + 语法分析)        │
└──────────┬───────────────┘
           ↓
┌──────────────────────────┐
│  2. Resolver 语义分析     │ → Stmt (Statement)
│  (转换为内部数据结构)     │
└──────────┬───────────────┘
           ↓
┌──────────────────────────┐
│  3. Transformer 转换      │ → LogicalOperator
│  (生成逻辑计划)          │
└──────────┬───────────────┘
           ↓
┌──────────────────────────┐
│  4. Optimizer 优化       │ → PhysicalOperator
│  (生成物理执行计划)      │   或 CommandExecutor
└──────────┬───────────────┘
           ↓
┌──────────────────────────┐
│  5. Executor 执行        │ → SqlResult
│  (执行并返回结果)        │
└──────────────────────────┘
```

---

## 🏗️ 五阶段开发流程详解

### **阶段1：Parser 解析阶段** 📝

**文件位置**：
- `src/observer/sql/parser/lex_sql.l` - 词法分析
- `src/observer/sql/parser/yacc_sql.y` - 语法分析
- `src/observer/sql/parser/parse_defs.h` - 数据结构定义

**实现步骤**：

#### 1.1 添加词法规则（lex_sql.l）

```c
// 例如：添加新的关键字
"GROUP"      { return GROUP; }
"BY"         { return BY; }
"ORDER"      { return ORDER; }
"TEXT"       { return TEXT; }
"UNIQUE"     { return UNIQUE; }
```

#### 1.2 定义语法规则（yacc_sql.y）

```yacc
// 例如：GROUP BY 的语法规则
group_by:
    /* empty */ {
        $$ = nullptr;
    }
    | GROUP BY expression_list {
        $$ = $3;
    }
    ;
```

#### 1.3 定义数据结构（parse_defs.h）

```cpp
// 解析后的数据结构
struct GroupBySqlNode {
    std::vector<Expression *> group_by_fields;
    std::vector<Expression *> aggregate_fields;
};
```

**调试技巧**：
```cpp
// 设置断点
src/observer/sql/parser/parse_stage.cpp:30
// 查看解析后的 ParsedSqlNode
```

---

### **阶段2：Resolver 语义分析** 🔍

**文件位置**：
- `src/observer/sql/stmt/` - 各种 Stmt 类
- `src/observer/sql/parser/resolve_stage.cpp` - 解析入口

**实现步骤**：

#### 2.1 创建 Stmt 类

```cpp
// 例如：insert_stmt.h
class InsertStmt : public Stmt {
public:
    InsertStmt(Table *table, const std::vector<InsertTuple> tuples);
    
    // 核心方法：从 ParsedSqlNode 创建 Stmt
    static RC create(Db *db, const InsertSqlNode &inserts, Stmt *&stmt);
    
    StmtType type() const override { return StmtType::INSERT; }
    
private:
    Table *table_;
    std::vector<InsertTuple> tuples_;
};
```

#### 2.2 实现 create 方法

```cpp
RC InsertStmt::create(Db *db, const InsertSqlNode &inserts, Stmt *&stmt)
{
    // 1. 验证表是否存在
    Table *table = db->find_table(table_name);
    if (nullptr == table) {
        return RC::SCHEMA_TABLE_NOT_EXIST;
    }
    
    // 2. 验证字段数量
    if (field_num != value_num) {
        return RC::SCHEMA_FIELD_MISSING;
    }
    
    // 3. 验证类型匹配
    for (int i = 0; i < field_num; i++) {
        // 类型检查和转换
        RC rc = Value::cast_to(values[i], field_type, real_value);
        if (OB_FAIL(rc)) {
            return rc;
        }
    }
    
    // 4. 创建 Stmt 对象
    stmt = new InsertStmt(table, inserts.tuples);
    return RC::SUCCESS;
}
```

#### 2.3 注册 Stmt 类型

```cpp
// stmt.h - 添加枚举类型
enum class StmtType {
    INSERT,
    SELECT,
    UPDATE,
    DELETE,
    // ... 其他类型
};

// stmt.cpp - 注册创建函数
RC Stmt::create_stmt(Db *db, ParsedSqlNode &sql_node, Stmt *&stmt)
{
    switch (sql_node.flag) {
        case SCF_INSERT:
            return InsertStmt::create(db, sql_node.insertion, stmt);
        case SCF_SELECT:
            return SelectStmt::create(db, sql_node.selection, stmt);
        // ... 其他类型
    }
}
```

**调试技巧**：
```cpp
// 设置断点
src/observer/sql/stmt/insert_stmt.cpp:25
// 查看 Stmt 对象的创建过程
```

---

### **阶段3：Transformer 转换为逻辑计划** 🎨

**文件位置**：
- `src/observer/sql/operator/*_logical_operator.h/cpp` - 逻辑算子
- `src/observer/sql/optimizer/logical_plan_generator.cpp` - 生成器

**实现步骤**：

#### 3.1 创建逻辑算子类

```cpp
// 例如：group_by_logical_operator.h
class GroupByLogicalOperator : public LogicalOperator {
public:
    GroupByLogicalOperator(
        vector<unique_ptr<Expression>> group_by_exprs,
        vector<unique_ptr<Expression>> aggregate_exprs
    );
    
    LogicalOperatorType type() const override { 
        return LogicalOperatorType::GROUP_BY; 
    }
    
    vector<unique_ptr<Expression>> &group_by_expressions() { 
        return group_by_expressions_; 
    }
    
private:
    vector<unique_ptr<Expression>> group_by_expressions_;
    vector<unique_ptr<Expression>> aggregate_expressions_;
};
```

#### 3.2 在生成器中创建逻辑算子

```cpp
// logical_plan_generator.cpp
RC LogicalPlanGenerator::create_plan(
    SelectStmt *select_stmt, 
    unique_ptr<LogicalOperator> &logical_operator)
{
    // 1. 创建表扫描算子
    // 2. 添加过滤条件
    // 3. 添加 GROUP BY 算子
    if (select_stmt->has_group_by()) {
        auto group_by_oper = make_unique<GroupByLogicalOperator>(
            std::move(group_by_exprs),
            std::move(aggregate_exprs)
        );
        group_by_oper->add_child(std::move(child_oper));
        logical_operator = std::move(group_by_oper);
    }
    // 4. 添加投影算子
    // 5. 添加排序算子
    
    return RC::SUCCESS;
}
```

**调试技巧**：
```cpp
// 设置断点
src/observer/sql/optimizer/logical_plan_generator.cpp:100
// 查看逻辑计划的构建过程
```

---

### **阶段4：Optimizer 生成物理计划** ⚙️

**文件位置**：
- `src/observer/sql/operator/*_physical_operator.h/cpp` - 物理算子
- `src/observer/sql/optimizer/physical_plan_generator.cpp` - 生成器

**实现步骤**：

#### 4.1 创建物理算子类

```cpp
// 例如：insert_physical_operator.h
class InsertPhysicalOperator : public PhysicalOperator {
public:
    InsertPhysicalOperator(Table *table, vector<InsertTuple> &&tuples);
    
    PhysicalOperatorType type() const override { 
        return PhysicalOperatorType::INSERT; 
    }
    
    // 核心方法
    RC open(Trx *trx) override;  // 初始化和执行
    RC next() override;           // 获取下一条记录
    RC close() override;          // 清理资源
    
private:
    Table *table_;
    vector<InsertTuple> tuples_;
};
```

#### 4.2 实现 open 方法（核心执行逻辑）

```cpp
RC InsertPhysicalOperator::open(Trx *trx)
{
    std::vector<Record *> inserted_record;
    
    // 遍历所有要插入的元组
    for (auto value : tuples_) {
        Record record;
        
        // 1. 创建记录
        RC rc = table_->make_record(
            static_cast<int>(value.size()), 
            value.data(), 
            record
        );
        if (rc != RC::SUCCESS) {
            return rc;
        }
        
        // 2. 通过事务插入
        rc = trx->insert_record(table_, record);
        if (rc != RC::SUCCESS) {
            // 3. 失败回滚
            for (Record *rec : inserted_record) {
                trx->delete_record(table_, *rec);
            }
            return rc;
        }
        
        inserted_record.emplace_back(new Record(record));
    }
    
    return RC::SUCCESS;
}
```

#### 4.3 注册物理算子生成

```cpp
// physical_plan_generator.cpp
RC PhysicalPlanGenerator::create(
    LogicalOperator &logical_oper, 
    unique_ptr<PhysicalOperator> &oper)
{
    switch (logical_oper.type()) {
        case LogicalOperatorType::GROUP_BY:
            return create_plan(
                static_cast<GroupByLogicalOperator &>(logical_oper), 
                oper
            );
        // ... 其他类型
    }
}

RC PhysicalPlanGenerator::create_plan(
    GroupByLogicalOperator &logical_oper, 
    unique_ptr<PhysicalOperator> &oper)
{
    // 根据是否有 GROUP BY 字段选择算子
    if (logical_oper.group_by_expressions().empty()) {
        // 标量聚合（如：SELECT COUNT(*) FROM t）
        oper = make_unique<ScalarGroupByPhysicalOperator>(...);
    } else {
        // 分组聚合（如：SELECT COUNT(*) FROM t GROUP BY col）
        oper = make_unique<HashGroupByPhysicalOperator>(...);
    }
    
    return RC::SUCCESS;
}
```

**调试技巧**：
```cpp
// 设置断点
src/observer/sql/operator/insert_physical_operator.cpp:26
// 查看物理算子的执行过程
```

---

### **阶段5：Executor 执行并返回结果** 🚀

**文件位置**：
- `src/observer/sql/executor/execute_stage.cpp` - DML 执行
- `src/observer/sql/executor/command_executor.cpp` - DDL 执行

**实现模式**：

#### 5.1 DML 语句（SELECT/INSERT/UPDATE/DELETE）

```cpp
// execute_stage.cpp
RC ExecuteStage::handle_request(SQLStageEvent *sql_event)
{
    // 1. 获取物理算子
    unique_ptr<PhysicalOperator> &physical_operator = 
        sql_event->physical_operator();
    
    // 2. 打开算子（执行）
    RC rc = physical_operator->open(trx);
    if (rc != RC::SUCCESS) {
        return rc;
    }
    
    // 3. 获取结果（火山模型）
    while (RC::SUCCESS == (rc = physical_operator->next())) {
        Tuple *tuple = physical_operator->current_tuple();
        // 处理每一行结果
    }
    
    // 4. 关闭算子
    physical_operator->close();
    
    return rc;
}
```

#### 5.2 DDL 语句（CREATE/DROP/ALTER）

```cpp
// command_executor.cpp
RC CommandExecutor::execute(SQLStageEvent *sql_event)
{
    Stmt *stmt = sql_event->stmt();
    
    switch (stmt->type()) {
        case StmtType::CREATE_TABLE: {
            CreateTableExecutor executor;
            rc = executor.execute(sql_event);
        } break;
        
        case StmtType::DROP_TABLE: {
            DropTableExecutor executor;
            rc = executor.execute(sql_event);
        } break;
        
        // ... 其他DDL命令
    }
    
    // DDL 后同步元数据
    if (OB_SUCC(rc) && stmt_type_ddl(stmt->type())) {
        rc = sql_event->session()->get_current_db()->sync();
    }
    
    return rc;
}
```

**调试技巧**：
```cpp
// 设置断点
src/observer/sql/executor/execute_stage.cpp:50
// 查看执行结果的生成过程
```

---

## 📊 各功能实现分析

### **1. INSERT（多行插入）**

**实现要点**：
- ✅ 支持单条插入多个值：`INSERT INTO t VALUES (1), (2), (3);`
- ✅ 原子性：全部成功或全部失败
- ✅ 事务管理：通过 `Trx` 管理插入和回滚

**关键代码**：
```cpp
// insert_physical_operator.cpp
for (auto value : tuples_) {
    rc = trx->insert_record(table_, record);
    if (rc != RC::SUCCESS) {
        // 回滚已插入的记录
        for (Record *rec : inserted_record) {
            trx->delete_record(table_, *rec);
        }
        return rc;
    }
}
```

---

### **2. GROUP BY + 聚合函数**

**实现要点**：
- ✅ 两种算子：`ScalarGroupByPhysicalOperator`（无GROUP BY） 和 `HashGroupByPhysicalOperator`（有GROUP BY）
- ✅ 支持聚合函数：COUNT, SUM, AVG, MAX, MIN
- ✅ 使用哈希表分组

**关键代码**：
```cpp
// hash_group_by_physical_operator.cpp
while (OB_SUCC(rc = child.next())) {
    // 1. 找到对应的 group
    GroupType *found_group = find_group(*child_tuple);
    
    // 2. 计算聚合值
    rc = aggregate(found_group->aggregators, tuple);
}

// 3. 计算最终结果
for (GroupType &group : groups_) {
    rc = evaluate(group.aggregators);
}
```

---

### **3. ORDER BY**

**实现要点**：
- ✅ 支持 ASC/DESC
- ✅ 支持多列排序
- ✅ 内存排序（收集所有数据后排序）

**关键代码**：
```cpp
// order_by_physical_operator.cpp
RC OrderByPhysicalOperator::open(Trx *trx)
{
    // 1. 收集所有数据
    while (OB_SUCC(child.next())) {
        tuples_.emplace_back(child.current_tuple()->clone());
    }
    
    // 2. 排序
    std::sort(tuples_.begin(), tuples_.end(), 
        [this](const Tuple *a, const Tuple *b) {
            return compare_tuple(a, b) < 0;
        }
    );
    
    return RC::SUCCESS;
}
```

---

### **4. JOIN（INNER JOIN）**

**实现要点**：
- ✅ 嵌套循环连接（Nested Loop Join）
- ✅ 支持多表 JOIN
- ✅ ON 条件过滤

**关键代码**：
```cpp
// join_physical_operator.cpp
RC JoinPhysicalOperator::next()
{
    // 嵌套循环
    while (true) {
        if (right_tuple_ == nullptr) {
            // 从左表获取下一条
            RC rc = left_->next();
            if (rc != RC::SUCCESS) return rc;
            
            // 重置右表
            right_->open(trx_);
        }
        
        // 从右表获取下一条
        RC rc = right_->next();
        if (rc == RC::SUCCESS) {
            // 检查 JOIN 条件
            if (evaluate_join_condition()) {
                return RC::SUCCESS;  // 找到匹配
            }
        } else {
            // 右表遍历完，获取左表下一条
            right_tuple_ = nullptr;
        }
    }
}
```

---

### **5. DATE 类型**

**实现要点**：
- ✅ 新增数据类型：`AttrType::DATE`
- ✅ 日期验证：闰年、月份天数
- ✅ 日期比较和索引

**实现流程**：
```
1. lex_sql.l     → 识别日期字符串 '2024-01-01'
2. yacc_sql.y    → 解析为 DATE 类型
3. date_type.cpp → 验证和存储（转换为 int32_t）
4. table.cpp     → 支持 DATE 字段的创建和查询
5. bplus_tree.cpp→ 支持 DATE 索引
```

---

### **6. TEXT 类型（4096字节）**

**实现要点**：
- ✅ 变长存储：不同于定长 CHAR
- ✅ 超长截断：超过 4096 字节截断
- ✅ 需要调整 `record_manager` 的实现

**关键修改**：
```cpp
// record_manager.cpp
// 变长记录格式：
// [record_size(4B)] [field1_len(4B)] [field1_data] [field2_len(4B)] ...
```

---

### **7. NULL 值支持**

**实现要点**：
- ✅ 字段属性：`NULLABLE` / `NOT NULL`
- ✅ NULL 比较规则：NULL 与任何值比较都是 FALSE
- ✅ 聚合函数：忽略 NULL 值

**关键代码**：
```cpp
// insert_stmt.cpp
if (values[i].is_null()) {
    if (not field_meta->nullable()) {
        return RC::NULL_CANT_INSERT;  // 不允许 NULL
    }
}

// aggregator.cpp
void Aggregator::aggregate(const Value &value) {
    if (value.is_null()) {
        return;  // 跳过 NULL 值
    }
    // ... 正常聚合
}
```

---

### **8. UNIQUE 索引**

**实现要点**：
- ✅ 创建索引时标记为 UNIQUE
- ✅ 插入时检查重复
- ✅ B+树查找重复键

**关键代码**：
```cpp
// bplus_tree_index.cpp
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
    // 1. 检查是否已存在（UNIQUE 约束）
    if (is_unique_) {
        std::vector<RID> rids;
        RC rc = tree_->scan_equal(key, rids);
        if (rc == RC::SUCCESS && !rids.empty()) {
            return RC::DUPLICATE_KEY;  // 重复键
        }
    }
    
    // 2. 插入索引
    return tree_->insert(key, rid);
}
```

---

### **9. 子查询（SUB-QUERY）**

**实现要点**：
- ✅ IN / NOT IN 子查询
- ✅ 比较运算子查询（>, <, =）
- ✅ 带聚合函数的子查询

**实现方式**：
```cpp
// 1. 子查询作为表达式
class SubqueryExpression : public Expression {
    RC evaluate(const Tuple &tuple, Value &result) override {
        // 执行子查询
        RC rc = physical_operator_->open(trx_);
        // 收集结果
        while (physical_operator_->next() == RC::SUCCESS) {
            results.push_back(physical_operator_->current_tuple());
        }
        return RC::SUCCESS;
    }
};

// 2. 在谓词中使用
if (value IN subquery_results) {
    // ...
}
```

---

### **10. UPDATE**

**实现要点**：
- ✅ 支持带条件的更新
- ✅ 更新索引
- ✅ 事务管理

**关键代码**：
```cpp
// update_physical_operator.cpp
RC UpdatePhysicalOperator::open(Trx *trx)
{
    // 1. 扫描需要更新的记录
    while (child_->next() == RC::SUCCESS) {
        Record *old_record = child_->current_tuple()->record();
        
        // 2. 创建新记录
        Record new_record = make_updated_record(old_record);
        
        // 3. 删除旧记录
        trx->delete_record(table_, *old_record);
        
        // 4. 插入新记录
        trx->insert_record(table_, new_record);
        
        // 5. 更新索引
        update_indexes(old_record, new_record);
    }
    
    return RC::SUCCESS;
}
```

---

## 🔍 系统化调试方案

### **调试流程图**

```
发现问题
    ↓
确定问题阶段（Parser/Resolver/Optimizer/Executor）
    ↓
设置断点
    ↓
F5 启动调试
    ↓
输入测试 SQL
    ↓
单步调试（F10/F11）
    ↓
查看变量值
    ↓
定位问题
    ↓
修复并验证
```

### **各阶段调试断点**

#### **1. Parser 阶段**
```cpp
// 断点位置：
src/observer/sql/parser/parse_stage.cpp:30

// 查看内容：
- sql_event->sql()  // 原始 SQL
- parsed_sql_node   // 解析后的节点

// 常见问题：
- 语法错误
- 关键字未识别
- 解析规则不匹配
```

#### **2. Resolver 阶段**
```cpp
// 断点位置：
src/observer/sql/stmt/xxx_stmt.cpp:create方法

// 查看内容：
- table->name()       // 表是否存在
- field_meta->type()  // 字段类型
- value_num vs field_num  // 数量匹配

// 常见问题：
- 表不存在
- 字段数量不匹配
- 类型不匹配
```

#### **3. Optimizer 阶段**
```cpp
// 断点位置：
src/observer/sql/optimizer/physical_plan_generator.cpp:100

// 查看内容：
- logical_operator->type()  // 逻辑算子类型
- physical_operator          // 生成的物理算子

// 常见问题：
- 算子选择错误
- 优化规则问题
```

#### **4. Executor 阶段**
```cpp
// 断点位置：
src/observer/sql/operator/xxx_physical_operator.cpp:open方法

// 查看内容：
- tuples_              // 数据内容
- current_tuple()      // 当前记录
- child_->next() 返回值  // 子算子状态

// 常见问题：
- 数据处理错误
- 算子逻辑错误
- 资源泄漏
```

### **调试技巧表**

| 问题类型 | 断点位置 | 查看内容 | 解决方向 |
|---------|---------|---------|---------|
| SQL 解析失败 | `parse_stage.cpp:30` | `parsed_sql_node` | 检查语法规则 |
| 表不存在 | `xxx_stmt.cpp:create` | `db->find_table()` | 检查表名 |
| 类型不匹配 | `xxx_stmt.cpp:create` | `field_type vs value_type` | 类型转换 |
| 查询结果错误 | `xxx_physical_operator.cpp:next` | `current_tuple()` | 算子逻辑 |
| 段错误 | 崩溃前的最后调用 | `bt` (GDB) | 空指针/数组越界 |
| 性能问题 | 算子 `open` 和 `next` | 循环次数 | 算法优化 |

---

## 💡 开发思路总结

基于 MiniOB 代码分析，总结出以下**通用开发思路**：

### **1. 分层设计原则** 🏗️

```
每一层只关注自己的职责：
- Parser：   只负责解析，不关心语义
- Resolver： 只负责验证和转换，不关心执行
- Optimizer：只负责优化，不关心底层实现
- Executor： 只负责执行，不关心优化逻辑
```

### **2. 接口驱动开发** 🔌

```cpp
// 1. 定义接口
class PhysicalOperator {
    virtual RC open(Trx *trx) = 0;
    virtual RC next() = 0;
    virtual RC close() = 0;
};

// 2. 实现具体算子
class InsertPhysicalOperator : public PhysicalOperator {
    RC open(Trx *trx) override { /* ... */ }
    RC next() override { /* ... */ }
    RC close() override { /* ... */ }
};

// 3. 统一调用方式
physical_operator->open(trx);
while (physical_operator->next() == RC::SUCCESS) {
    // 处理结果
}
physical_operator->close();
```

### **3. 数据驱动设计** 📊

```cpp
// 不是写死逻辑，而是通过数据驱动
enum class StmtType {
    INSERT, SELECT, UPDATE, DELETE, ...
};

RC Stmt::create_stmt(Db *db, ParsedSqlNode &node, Stmt *&stmt) {
    switch (node.flag) {
        case SCF_INSERT: return InsertStmt::create(...);
        case SCF_SELECT: return SelectStmt::create(...);
        // 添加新功能只需要加一个 case
    }
}
```

### **4. 组合优于继承** 🧩

```cpp
// 算子通过组合形成执行树
auto project_oper = make_unique<ProjectPhysicalOperator>();
auto filter_oper = make_unique<PredicatePhysicalOperator>();
auto scan_oper = make_unique<TableScanPhysicalOperator>();

// 组合成执行计划
project_oper->add_child(std::move(filter_oper));
filter_oper->add_child(std::move(scan_oper));

// 执行时从上到下调用
project_oper->open(trx);  // 会递归调用子算子
```

### **5. 错误处理原则** ❌

```cpp
// 1. 早检查，早返回
if (table == nullptr) {
    return RC::SCHEMA_TABLE_NOT_EXIST;
}

// 2. 详细的错误信息
LOG_WARN("failed to insert record. table=%s, rc=%s", 
    table->name(), strrc(rc));

// 3. 资源清理
if (rc != RC::SUCCESS) {
    for (Record *rec : inserted_records) {
        delete rec;  // 清理已分配的资源
    }
    return rc;
}
```

### **6. 渐进式开发** 📈

```
第1步：实现基础功能
  └─ 先让功能跑通，不考虑优化

第2步：完善错误处理
  └─ 添加各种边界情况检查

第3步：性能优化
  └─ 使用更高效的算法和数据结构

第4步：代码重构
  └─ 提取公共逻辑，简化代码

第5步：添加测试
  └─ 回归测试，防止破坏已有功能
```

### **7. 测试驱动开发** 🧪

```sql
-- 1. 先写测试用例
CREATE TABLE test(id INT, name CHAR(20));
INSERT INTO test VALUES (1, 'Alice');
SELECT * FROM test;  -- 预期：返回 1行

-- 2. 实现功能
// 实现 INSERT 的各个阶段

-- 3. 运行测试
按 F5 → 输入 SQL → 检查结果

-- 4. 修复问题
设置断点 → 单步调试 → 定位问题

-- 5. 重新测试
确保所有测试通过
```

---

## 🎯 添加新功能的标准流程

### **Step 1：分析需求**
```
- 新功能是什么？（如：GROUP BY）
- 涉及哪些 SQL 语法？
- 需要哪些数据结构？
- 有哪些边界情况？
```

### **Step 2：设计方案**
```
- 参考类似功能的实现
- 确定需要修改的文件
- 画出数据流图
- 设计测试用例
```

### **Step 3：实现 Parser**
```
1. 在 lex_sql.l 添加词法
2. 在 yacc_sql.y 添加语法规则
3. 在 parse_defs.h 定义数据结构
4. 测试：能否正确解析
```

### **Step 4：实现 Resolver**
```
1. 创建 XXXStmt 类
2. 实现 create 方法（验证逻辑）
3. 注册到 Stmt::create_stmt
4. 测试：能否正确验证
```

### **Step 5：实现 Optimizer**
```
1. 创建 XXXLogicalOperator
2. 在 LogicalPlanGenerator 中生成
3. 创建 XXXPhysicalOperator
4. 在 PhysicalPlanGenerator 中生成
5. 测试：执行计划是否正确
```

### **Step 6：实现 Executor**
```
1. 实现 PhysicalOperator::open
2. 实现 PhysicalOperator::next
3. 实现 PhysicalOperator::close
4. 测试：结果是否正确
```

### **Step 7：完善和优化**
```
1. 添加错误处理
2. 处理边界情况
3. 性能优化
4. 代码review
5. 回归测试
```

---

## 📝 开发检查清单

### **代码质量**
- [ ] 遵循五阶段流程
- [ ] 错误处理完善
- [ ] 日志信息详细
- [ ] 资源正确释放
- [ ] 代码风格一致

### **功能完整性**
- [ ] 基本功能实现
- [ ] 边界情况处理
- [ ] 错误情况处理
- [ ] 性能可接受

### **测试覆盖**
- [ ] 正常情况测试
- [ ] 边界情况测试
- [ ] 错误情况测试
- [ ] 回归测试通过

---

## 🎓 学习建议

1. **从简单功能开始**
   - 先理解 INSERT 的实现
   - 再看 SELECT 的实现
   - 最后看 JOIN 的实现

2. **跟踪完整流程**
   - 选一个功能（如 INSERT）
   - 从 Parser 到 Executor 完整跟踪
   - 理解每个阶段的作用

3. **对比学习**
   - 对比不同功能的实现
   - 找出共同模式
   - 理解设计思想

4. **动手实践**
   - 尝试添加新功能
   - 修复已知问题
   - 优化现有实现

---

## 🚀 总结

MiniOB 的开发遵循**统一的五阶段模式**，每个功能都经历：

```
Parser → Resolver → Transformer → Optimizer → Executor
```

**核心思想**：
1. 分层设计，职责明确
2. 接口驱动，易于扩展
3. 数据驱动，灵活配置
4. 组合优先，复用性强
5. 测试驱动，质量保证

掌握这套模式后，实现新功能就像**填空题**一样：
- Parser：     定义语法 ✓
- Resolver：   验证和转换 ✓
- Optimizer：  生成计划 ✓
- Executor：   执行逻辑 ✓

**记住**：MiniOB 已经提供了完整的框架，你只需要在每个阶段"填入"你的功能逻辑即可！

祝你开发顺利！🎉

