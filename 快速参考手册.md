# ⚡ MiniOB 快速参考指南

## 🎯 五阶段速查表

| 阶段 | 文件位置 | 主要工作 | 输出 |
|------|---------|---------|------|
| **1. Parser** | `lex_sql.l`<br>`yacc_sql.y`<br>`parse_defs.h` | 词法+语法分析 | `ParsedSqlNode` |
| **2. Resolver** | `src/observer/sql/stmt/` | 语义验证+转换 | `Stmt` |
| **3. Transformer** | `src/observer/sql/optimizer/`<br>`logical_plan_generator.cpp` | 生成逻辑计划 | `LogicalOperator` |
| **4. Optimizer** | `src/observer/sql/optimizer/`<br>`physical_plan_generator.cpp` | 生成物理计划 | `PhysicalOperator` |
| **5. Executor** | `src/observer/sql/executor/`<br>`execute_stage.cpp` | 执行并返回结果 | `SqlResult` |

---

## 🔍 调试断点速查

| 问题类型 | 断点文件 | 断点行 | 查看内容 |
|---------|---------|-------|----------|
| SQL 解析 | `parse_stage.cpp` | 30 | `parsed_sql_node` |
| 表验证 | `xxx_stmt.cpp` | create 方法 | `table->name()` |
| 逻辑计划 | `logical_plan_generator.cpp` | 100 | `logical_operator` |
| 物理计划 | `physical_plan_generator.cpp` | 50 | `physical_operator` |
| 执行过程 | `xxx_physical_operator.cpp` | open 方法 | `tuples_` |
| 结果返回 | `execute_stage.cpp` | 50 | `sql_result` |

---

## 📂 关键文件清单

### **核心框架**
```
src/observer/sql/
├── parser/
│   ├── lex_sql.l              # 词法规则
│   ├── yacc_sql.y             # 语法规则
│   ├── parse_defs.h           # 解析数据结构
│   ├── parse_stage.cpp        # 解析入口
│   └── resolve_stage.cpp      # 语义分析入口
│
├── stmt/
│   ├── stmt.h                 # Stmt 基类
│   ├── insert_stmt.cpp        # INSERT 语句
│   ├── select_stmt.cpp        # SELECT 语句
│   └── ...                    # 其他语句
│
├── optimizer/
│   ├── logical_plan_generator.cpp   # 逻辑计划生成
│   ├── physical_plan_generator.cpp  # 物理计划生成
│   └── optimize_stage.cpp           # 优化入口
│
├── operator/
│   ├── physical_operator.h          # 物理算子基类
│   ├── insert_physical_operator.cpp # INSERT 算子
│   ├── join_physical_operator.cpp   # JOIN 算子
│   ├── group_by_physical_operator.cpp # GROUP BY 算子
│   └── ...                          # 其他算子
│
└── executor/
    ├── execute_stage.cpp            # 执行入口
    └── command_executor.cpp         # DDL 命令执行
```

---

## 🔧 常用命令

### **编译和运行**
```bash
# 编译
bash build.sh debug

# F5 调试（VSCode）
按 F5 → DEBUG CONSOLE → 输入 SQL

# CLI 模式
cd build_debug
./bin/observer -f ../etc/observer.ini -P cli

# 客户端模式
# 终端1
./bin/observer -f ../etc/observer.ini -s miniob.sock
# 终端2
./bin/obclient -s miniob.sock

# 自动化测试
cd test/case
python3 miniob_test.py --test-cases=basic
```

### **GDB 调试**
```bash
gdb ./build_debug/bin/observer
(gdb) run -f ../etc/observer.ini -P cli
(gdb) break insert_stmt.cpp:25
(gdb) continue
(gdb) print *table
(gdb) next   # 下一行
(gdb) step   # 进入函数
(gdb) bt     # 查看调用栈
```

---

## 📊 功能实现速查

### **INSERT**
```
Parser:    yacc_sql.y (insert_stmt 规则)
Resolver:  insert_stmt.cpp (验证表、字段、类型)
Optimizer: insert_physical_operator.cpp (生成算子)
Executor:  insert_physical_operator.cpp::open (执行插入)
```

### **GROUP BY**
```
Parser:    yacc_sql.y (group_by 规则)
Resolver:  select_stmt.cpp (处理 GROUP BY 字段)
Optimizer: group_by_logical_operator.cpp (逻辑算子)
           hash_group_by_physical_operator.cpp (物理算子)
Executor:  hash_group_by_physical_operator.cpp::open (分组聚合)
```

### **JOIN**
```
Parser:    yacc_sql.y (join_tables 规则)
Resolver:  select_stmt.cpp (处理 JOIN 条件)
Optimizer: join_logical_operator.cpp (逻辑算子)
           join_physical_operator.cpp (嵌套循环连接)
Executor:  join_physical_operator.cpp::next (逐行匹配)
```

### **ORDER BY**
```
Parser:    yacc_sql.y (order_by 规则)
Resolver:  select_stmt.cpp (处理排序字段)
Optimizer: order_by_physical_operator.cpp (排序算子)
Executor:  order_by_physical_operator.cpp::open (收集+排序)
```

### **DATE 类型**
```
Parser:    lex_sql.l (识别日期字符串)
           yacc_sql.y (DATE 类型)
Resolver:  date_type.cpp (验证日期)
Storage:   date_type.cpp (存储为 int32_t)
Execute:   date_type.cpp (日期比较)
```

### **NULL 支持**
```
Parser:    yacc_sql.y (NULLABLE 关键字)
Resolver:  xxx_stmt.cpp (检查 nullable 属性)
Storage:   field_meta.cpp (nullable 标志)
Execute:   value.cpp (NULL 值处理)
```

---

## 🎯 添加新功能 Checklist

### **Step 1: Parser (词法+语法)**
- [ ] `lex_sql.l` - 添加新关键字
- [ ] `yacc_sql.y` - 添加语法规则
- [ ] `parse_defs.h` - 定义数据结构
- [ ] 测试：能否解析 SQL

### **Step 2: Resolver (语义分析)**
- [ ] 创建 `xxx_stmt.h/cpp`
- [ ] 实现 `create` 方法（验证逻辑）
- [ ] 注册到 `Stmt::create_stmt`
- [ ] 测试：能否通过验证

### **Step 3: Optimizer (生成计划)**
- [ ] 创建 `xxx_logical_operator.h/cpp`
- [ ] 在 `logical_plan_generator.cpp` 中生成
- [ ] 创建 `xxx_physical_operator.h/cpp`
- [ ] 在 `physical_plan_generator.cpp` 中生成
- [ ] 测试：计划是否正确

### **Step 4: Executor (执行)**
- [ ] 实现 `open()`  - 初始化和主要逻辑
- [ ] 实现 `next()`  - 获取下一条记录
- [ ] 实现 `close()` - 清理资源
- [ ] 测试：结果是否正确

### **Step 5: 完善**
- [ ] 添加错误处理
- [ ] 处理边界情况
- [ ] 性能优化
- [ ] 回归测试

---

## 🐛 常见问题速查

| 现象 | 可能原因 | 排查方法 |
|------|---------|---------|
| 编译错误 | 语法错误、缺少头文件 | 查看编译错误信息 |
| 段错误 | 空指针、数组越界 | `gdb bt` 查看调用栈 |
| SQL 解析失败 | 语法规则错误 | 断点 `parse_stage.cpp:30` |
| 表不存在 | 表名错误 | 断点 `xxx_stmt.cpp:create` |
| 类型不匹配 | 类型检查逻辑 | 查看 `field_type` vs `value_type` |
| 结果错误 | 算子逻辑错误 | 断点 `xxx_physical_operator.cpp:next` |
| 性能慢 | 算法效率低 | profiling 分析 |
| 内存泄漏 | 未释放资源 | valgrind 检查 |

---

## 💡 核心设计模式

### **1. 五阶段流水线**
```
SQL → Parser → Resolver → Optimizer → Executor → Result
```

### **2. 接口驱动**
```cpp
class PhysicalOperator {
    virtual RC open(Trx *trx) = 0;   // 初始化
    virtual RC next() = 0;           // 获取下一条
    virtual RC close() = 0;          // 清理
};
```

### **3. 火山模型**
```cpp
// 算子组合成树
project_op
  └─ filter_op
      └─ scan_op

// 从上到下递归调用
project_op->next()
  └─ filter_op->next()
      └─ scan_op->next()
```

### **4. 算子组合**
```cpp
auto project = make_unique<ProjectPhysicalOperator>();
auto filter = make_unique<PredicatePhysicalOperator>();
auto scan = make_unique<TableScanPhysicalOperator>();

project->add_child(std::move(filter));
filter->add_child(std::move(scan));
```

---

## 📝 代码模板

### **创建新的 Stmt**
```cpp
// xxx_stmt.h
class XxxStmt : public Stmt {
public:
    XxxStmt(...);
    static RC create(Db *db, const XxxSqlNode &node, Stmt *&stmt);
    StmtType type() const override { return StmtType::XXX; }
    
private:
    // 成员变量
};

// xxx_stmt.cpp
RC XxxStmt::create(Db *db, const XxxSqlNode &node, Stmt *&stmt)
{
    // 1. 验证表
    // 2. 验证字段
    // 3. 验证类型
    // 4. 创建 Stmt
    stmt = new XxxStmt(...);
    return RC::SUCCESS;
}
```

### **创建新的 PhysicalOperator**
```cpp
// xxx_physical_operator.h
class XxxPhysicalOperator : public PhysicalOperator {
public:
    XxxPhysicalOperator(...);
    
    PhysicalOperatorType type() const override { 
        return PhysicalOperatorType::XXX; 
    }
    
    RC open(Trx *trx) override;
    RC next() override;
    RC close() override;
    
private:
    // 成员变量
};

// xxx_physical_operator.cpp
RC XxxPhysicalOperator::open(Trx *trx)
{
    // 主要执行逻辑
    // 1. 打开子算子
    // 2. 处理数据
    // 3. 返回结果
    return RC::SUCCESS;
}

RC XxxPhysicalOperator::next()
{
    // 获取下一条记录
    // 火山模型
    return RC::RECORD_EOF;
}

RC XxxPhysicalOperator::close()
{
    // 清理资源
    return RC::SUCCESS;
}
```

---

## 🎓 学习路径

### **初级（1-2天）**
1. 理解五阶段流程
2. 跟踪一个 INSERT 语句
3. 修改一个简单功能

### **中级（3-5天）**
1. 理解所有算子的实现
2. 实现一个新的简单功能
3. 调试复杂问题

### **高级（1-2周）**
1. 实现复杂功能（如 JOIN）
2. 优化性能
3. 重构代码

---

## 🚀 快速开始

```bash
# 1. 读文档（30分钟）
cat IMPLEMENTATION_PATTERN.md
cat START_HERE.md

# 2. 编译项目
bash build.sh debug

# 3. 运行测试
# 按 F5 → 运行 test/case/test/basic.test

# 4. 设置断点调试
# insert_physical_operator.cpp:26

# 5. 跟踪完整流程
# Parser → Resolver → Optimizer → Executor
```

---

## 📞 文档索引

- **完整实现模式**：`IMPLEMENTATION_PATTERN.md` (超详细)
- **快速开始**：`QUICK_START_TEST.md`
- **官方测试使用**：`USE_OFFICIAL_TESTS.md`
- **测试策略**：`TESTING_STRATEGY.md`
- **总入口**：`START_HERE.md`
- **本文档**：`QUICK_REFERENCE.md` (快速参考)

---

**记住**：MiniOB 就像一条流水线，每个阶段负责一件事，理解这个模式后，实现新功能就很简单了！🎉

