# 💻 MiniOB 16个功能代码实现详解

## 🚀 F5 调试快速入门

### 调试环境配置

#### Windows + VSCode 配置

**1. 安装必要工具**：
- Visual Studio Code
- C/C++ 扩展
- CMake 扩展
- GDB 或 LLDB 调试器

**2. 创建 `.vscode/launch.json`**：
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "调试 Observer",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/observer",
      "args": ["-f", "${workspaceFolder}/etc/observer.ini"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "setupCommands": [
        {
          "description": "为 gdb 启用整齐打印",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "cmake-build-debug"
    }
  ]
}
```

**3. 创建 `.vscode/tasks.json`**：
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cmake-build-debug",
      "type": "shell",
      "command": "cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make -j4",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
```

#### Linux 配置

同上，但 `MIMode` 使用 `gdb`，并确保安装了 `gdb`：
```bash
sudo apt-get install gdb
```

#### Mac 配置

同上，但 `MIMode` 使用 `lldb`：
```bash
brew install lldb
```

### F5 调试基本操作

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| **F5** | 启动/继续调试 | 运行到下一个断点 |
| **F9** | 设置/取消断点 | 在当前行设置断点 |
| **F10** | 单步跳过 (Step Over) | 执行当前行，不进入函数内部 |
| **F11** | 单步进入 (Step Into) | 进入函数内部调试 |
| **Shift+F11** | 单步跳出 (Step Out) | 跳出当前函数 |
| **Ctrl+Shift+F5** | 重启调试 | 重新开始调试会话 |
| **Shift+F5** | 停止调试 | 结束调试会话 |

### 调试面板介绍

**左侧调试面板**：
- **变量 (Variables)**：查看当前作用域的所有变量
- **监视 (Watch)**：添加自定义表达式监控
- **调用堆栈 (Call Stack)**：查看函数调用链
- **断点 (Breakpoints)**：管理所有断点

### 实用调试技巧

#### 1. 条件断点
右键断点 → 编辑断点 → 输入条件：
```cpp
// 只在 id == 100 时中断
record.rid().page_num == 1 && record.rid().slot_num == 100
```

#### 2. 日志断点
右键断点 → 编辑断点 → 勾选"记录消息"：
```
插入记录 {record.rid().page_num}:{record.rid().slot_num}
```

#### 3. 数据断点（内存监视）
在变量上右键 → 中断于值更改

#### 4. 查看复杂对象
在 Watch 窗口输入表达式：
```cpp
table_->table_meta_.fields_[0]->name_    // 查看第一个字段名
*record.data()@20                        // 查看前20字节数据
```

### 常用断点位置速查

| 功能 | 文件 | 函数/位置 |
|------|------|----------|
| SQL 解析 | `sql_stage.cpp` | `ParseStage::handle_event()` |
| 语句验证 | `*_stmt.cpp` | `XXXStmt::create()` |
| 执行入口 | `*_executor.cpp` | `XXXExecutor::execute()` |
| 物理算子 | `*_physical_operator.cpp` | `open()` / `next()` |
| 表操作 | `table.cpp` | `insert_record()` / `delete_record()` |
| 索引操作 | `bplus_tree_index.cpp` | `insert_entry()` / `delete_entry()` |
| 事务操作 | `trx.cpp` | `insert_record()` / `commit()` |

### 调试实战演练

**第一步：启动调试**
1. 打开 VSCode
2. 打开 `src/observer/sql/executor/create_table_executor.cpp`
3. 在 `CreateTableExecutor::execute()` 第一行按 **F9** 设置断点
4. 按 **F5** 启动调试
5. 在命令行输入：`CREATE TABLE t1(id int);`

**第二步：观察执行流程**
- 程序会停在断点处
- 按 **F10** 单步执行
- 在左侧"变量"面板查看 `stmt`、`session` 等变量
- 将鼠标悬停在变量上查看详细信息

**第三步：深入函数内部**
- 当执行到 `db->create_table()` 时
- 按 **F11** 进入函数内部
- 继续单步调试，观察表是如何被创建的

**第四步：查看调用栈**
- 在左侧"调用堆栈"面板查看完整的函数调用链
- 点击任意栈帧可以查看该层的变量

## 📋 目录

- [1. basic - 基础功能](#1-basic---基础功能)
- [2. insert - 多行插入](#2-insert---多行插入)
- [3. select-meta - 元数据检查](#3-select-meta---元数据检查)
- [4. drop-table - 删除表](#4-drop-table---删除表)
- [5. date - 日期类型](#5-date---日期类型)
- [6. text - 长文本类型](#6-text---长文本类型)
- [7. null - NULL值支持](#7-null---null值支持)
- [8. multi-index - 多列索引](#8-multi-index---多列索引)
- [9. unique - 唯一索引](#9-unique---唯一索引)
- [10. select-tables - 多表查询](#10-select-tables---多表查询)
- [11. join-tables - INNER JOIN](#11-join-tables---inner-join)
- [12. order-by - 排序](#12-order-by---排序)
- [13. group-by - 分组](#13-group-by---分组)
- [14. aggregation-func - 聚合函数](#14-aggregation-func---聚合函数)
- [15. simple-sub-query - 子查询](#15-simple-sub-query---子查询)
- [16. update - 更新](#16-update---更新)

---

## 1. basic - 基础功能

### 📝 功能概述

基础功能包括：
- CREATE TABLE - 创建表
- INSERT - 插入数据
- SELECT - 查询数据
- DELETE - 删除数据
- CREATE INDEX - 创建索引
- CALC - 表达式计算

### 🗂️ 关键文件

```
Parser:    yacc_sql.y (各种SQL语法规则)
Resolver:  create_table_stmt.cpp, insert_stmt.cpp, select_stmt.cpp
Executor:  create_table_executor.cpp, insert_physical_operator.cpp
Storage:   table.cpp, record_manager.cpp, bplus_tree.cpp
```

### 💻 代码详解

### 🐛 调试指南 - CREATE TABLE

#### 断点位置设置

**调试 CREATE TABLE 的关键断点**：

1. **语法解析入口**：`src/observer/sql/parser/yacc_sql.y`
   - 搜索 `create_table_stmt:` 规则
   - 在 Bison 生成的 C 文件中实际断点位置：`src/observer/sql/parser/yacc_sql.cpp` (生成后)

2. **Resolver 入口**：`src/observer/sql/stmt/create_table_stmt.cpp`
   ```
   断点：CreateTableStmt::create() 函数第一行
   位置：验证表名、字段信息
   ```

3. **执行器入口**：`src/observer/sql/executor/create_table_executor.cpp`
   ```
   断点：CreateTableExecutor::execute() 函数第一行
   作用：查看 SQL 语句如何被执行
   ```

4. **存储层入口**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::create() 函数开始
   断点2：table_meta.init() 调用前
   断点3：RecordFileHandler::create() 调用前
   断点4：table_meta.serialize() 调用前
   ```

#### F5 调试步骤

**第一步：启动调试**
- VSCode：打开 `.vscode/launch.json`，配置好后按 F5
- Visual Studio：设置 observer 为启动项目，按 F5

**第二步：在命令行输入**
```sql
CREATE TABLE t1(id int, name char(10));
```

**第三步：观察调用栈**
```
main()
  → Server::serve()
    → SessionStage::handle_request()
      → ParseStage::handle_event()           ← 断点1：解析 SQL
        → yyparse()
      → ResolveStage::handle_event()         ← 断点2：创建 Stmt
        → CreateTableStmt::create()
      → ExecuteStage::handle_event()         ← 断点3：执行
        → CreateTableExecutor::execute()
          → Db::create_table()
            → Table::create()                ← 断点4：创建表文件
```

**第四步：检查变量**
- 在 `Table::create()` 中检查：
  - `name`：表名是否正确
  - `attribute_count`：字段数量
  - `attributes`：字段定义数组
  - `table_meta`：元数据对象

**第五步：单步调试关键代码**
```cpp
// 在 table.cpp 的 Table::create() 中
RC Table::create(...) {
  // ← 断点在这里，按F10单步执行
  TableMeta table_meta;
  RC rc = table_meta.init(name, attribute_count, attributes);
  // ← 检查 rc 的值，应该是 RC::SUCCESS
  
  if (rc != RC::SUCCESS) {
    // 如果执行到这里，说明元数据初始化失败
    return rc;
  }
  
  // ← 继续F10，观察数据文件创建
  std::string data_file = table_data_file(path, name);
  rc = RecordFileHandler::create(data_file.c_str(), ...);
  // ← 检查文件是否创建成功
}
```

#### 1.1 CREATE TABLE 实现

**语法解析（yacc_sql.y）**：

```yacc
create_table_stmt:
    CREATE TABLE ID LBRACE attr_def attr_def_list RBRACE storage_format {
        $$ = new ParsedSqlNode(SCF_CREATE_TABLE);
        CreateTableSqlNode &create_table = $$->create_table;
        create_table.relation_name = $3;
        
        // 处理字段定义列表
        std::vector<AttrInfoSqlNode> *src_attrs = $6;
        if (nullptr != src_attrs) {
            create_table.attr_infos.swap(*src_attrs);
        }
        create_table.attr_infos.emplace_back(*$5);
        std::reverse(create_table.attr_infos.begin(), 
                     create_table.attr_infos.end());
        delete $5;
    }
    ;

attr_def:
    ID type LBRACE number RBRACE nullable {
        $$ = new AttrInfoSqlNode;
        $$->type = (AttrType)$2;
        $$->name = $1;
        $$->length = $4;
        $$->nullable = $6;  // 是否允许NULL
        free($1);
    }
    | ID type nullable {
        $$ = new AttrInfoSqlNode;
        $$->type = (AttrType)$2;
        $$->name = $1;
        $$->length = 4;
        $$->nullable = $3;
        free($1);
    }
    ;
```

**执行器实现（create_table_executor.cpp）**：

```cpp
RC CreateTableExecutor::execute(SQLStageEvent *sql_event)
{
  Stmt    *stmt    = sql_event->stmt();
  Session *session = sql_event->session_event()->session();
  ASSERT(stmt->type() == StmtType::CREATE_TABLE, 
         "create table executor can not run this command: %d", 
         static_cast<int>(stmt->type()));

  CreateTableStmt *create_table_stmt = static_cast<CreateTableStmt *>(stmt);

  // 获取当前数据库
  Db *db = session->get_current_db();
  
  // 创建表
  RC rc = db->create_table(
      create_table_stmt->table_name().c_str(),    // 表名
      create_table_stmt->attr_infos().size(),      // 字段数量
      create_table_stmt->attr_infos().data()       // 字段信息
  );

  return rc;
}
```

**存储层实现（table.cpp）**：

```cpp
RC Table::create(Db *db, const char *name, 
                 int attribute_count, const AttrInfoSqlNode *attributes)
{
  // 1. 创建表元数据
  TableMeta table_meta;
  RC rc = table_meta.init(name, attribute_count, attributes);
  if (rc != RC::SUCCESS) {
    return rc;
  }

  // 2. 创建数据文件
  std::string data_file = table_data_file(path, name);
  rc = RecordFileHandler::create(data_file.c_str(), 
                                   table_meta.record_size());
  if (rc != RC::SUCCESS) {
    return rc;
  }

  // 3. 保存元数据到文件
  std::string meta_file = table_meta_file(path, name);
  std::fstream fs;
  fs.open(meta_file, std::ios::out | std::ios::binary);
  if (!fs.is_open()) {
    return RC::IOERR_OPEN;
  }
  
  // 序列化元数据
  table_meta.serialize(fs);
  fs.close();

  return RC::SUCCESS;
}
```

### 🐛 调试指南 - INSERT

#### 断点位置设置

**调试 INSERT 的关键断点**：

1. **Resolver**：`src/observer/sql/stmt/insert_stmt.cpp`
   ```
   断点1：InsertStmt::create() 函数开始
   断点2：类型检查循环中 (检查每个字段类型)
   ```

2. **执行器**：`src/observer/sql/operator/physical_operator/insert_physical_operator.cpp`
   ```
   断点1：InsertPhysicalOperator::open() 函数开始
   断点2：遍历 tuples_ 的循环开始处
   断点3：trx->insert_record() 调用前
   断点4：失败回滚的代码块
   ```

3. **表层**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::make_record() - 创建记录
   断点2：memcpy() 复制数据处 - 观察数据如何写入
   断点3：Table::insert_record() - 插入记录
   断点4：index->insert_entry() - 更新索引
   ```

4. **记录管理**：`src/observer/storage/record/record_manager.cpp`
   ```
   断点：RecordFileHandler::insert_record() - 分配页面和槽位
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
INSERT INTO t1 VALUES (1, 'hello');
```

**调用链追踪**：
```
InsertPhysicalOperator::open()
  ├─ Table::make_record()              ← 断点：观察Value如何转为二进制
  │   └─ memcpy(record_data + offset)  ← 断点：查看数据复制
  │
  ├─ trx->insert_record()              ← 断点：事务处理
  │   └─ Table::insert_record()
  │       ├─ record_handler_->insert_record()  ← 断点：分配RID
  │       └─ index->insert_entry()     ← 断点：索引插入
  │
  └─ [失败] trx->delete_record()       ← 断点：观察回滚逻辑
```

**重点观察变量**：
```cpp
// 在 InsertPhysicalOperator::open() 中
for (auto value : tuples_) {  // ← 断点，检查 tuples_ 内容
  // value 是一个 vector<Value>
  // 鼠标悬停查看每个 Value 的类型和数据
  
  Record record;
  RC rc = table_->make_record(...);  // ← F11 进入函数内部
  
  // 在 Table::make_record() 内部
  for (int i = 0; i < value_num; i++) {
    const Value &value = values[i];  // ← 检查每个值
    // 查看 value.attr_type(), value.data()
    
    memcpy(record_data + field->offset(), 
           value.data(), 
           copy_len);  // ← 断点：数据被复制到这里
  }
}
```

#### 1.2 INSERT 实现（单行）

**物理算子（insert_physical_operator.cpp）**：

```cpp
RC InsertPhysicalOperator::open(Trx *trx)
{
  std::vector<Record *> inserted_record;
  
  // 遍历所有要插入的元组
  for (auto value : tuples_) {
    Record record;
    
    // 1. 创建记录（将Value转换为二进制）
    RC rc = table_->make_record(
        static_cast<int>(value.size()),  // 字段数量
        value.data(),                     // 字段值数组
        record                            // 输出：记录对象
    );
    
    if (rc != RC::SUCCESS) {
      LOG_WARN("failed to make record. rc=%s", strrc(rc));
      return rc;
    }
    
    // 2. 通过事务插入记录
    rc = trx->insert_record(table_, record);
    if (rc != RC::SUCCESS) {
      LOG_WARN("failed to insert record by transaction. rc=%s", strrc(rc));
      
      // 3. 失败回滚：删除已插入的记录
      for (Record *rec : inserted_record) {
        trx->delete_record(table_, *rec);
      }
      return rc;
    }
    
    // 4. 记录已插入的记录（用于可能的回滚）
    inserted_record.emplace_back(new Record(record));
  }
  
  // 5. 清理临时记录
  for (Record *&rec : inserted_record) {
    delete rec;
  }
  
  return RC::SUCCESS;
}
```

**表层实现（table.cpp）**：

```cpp
RC Table::make_record(int value_num, const Value *values, Record &record)
{
  // 1. 计算记录大小
  int record_size = table_meta_.record_size();
  char *record_data = (char *)malloc(record_size);
  
  // 2. 初始化记录数据（全部设为0）
  memset(record_data, 0, record_size);

  // 3. 遍历每个字段，将Value转换为二进制格式
  for (int i = 0; i < value_num; i++) {
    const FieldMeta *field = table_meta_.field(i + sys_field_num);
    const Value     &value = values[i];
    
    // 计算字段在记录中的偏移量
    size_t copy_len = field->len();
    size_t data_len = copy_len;
    
    // 处理不同类型的数据
    if (field->type() == AttrType::CHARS) {
      // 字符串类型
      const size_t data_len = value.length();
      if (copy_len > data_len) {
        copy_len = data_len;
      }
    }
    
    // 复制数据到记录缓冲区
    memcpy(record_data + field->offset(), value.data(), copy_len);
  }

  // 4. 设置记录对象
  record.set_data_owner(record_data, record_size);
  
  return RC::SUCCESS;
}

RC Table::insert_record(Record &record)
{
  // 1. 插入到记录文件
  RC rc = record_handler_->insert_record(
      record.data(),   // 记录数据
      record.len(),    // 记录长度
      &record.rid()    // 输出：记录ID (页号+槽位)
  );
  
  if (rc != RC::SUCCESS) {
    return rc;
  }

  // 2. 更新所有索引
  for (Index *index : indexes_) {
    rc = index->insert_entry(record.data(), &record.rid());
    if (rc != RC::SUCCESS) {
      // 索引插入失败，删除记录并返回
      record_handler_->delete_record(&record.rid());
      return rc;
    }
  }

  return RC::SUCCESS;
}
```

### 🐛 调试指南 - SELECT

#### 断点位置设置

**调试 SELECT 的关键断点**：

1. **Resolver**：`src/observer/sql/stmt/select_stmt.cpp`
   ```
   断点1：SelectStmt::create() 函数开始
   断点2：验证表的循环
   断点3：验证字段的循环
   断点4：FilterStmt::create() 调用处
   ```

2. **优化器**：`src/observer/sql/optimizer/physical_plan_generator.cpp`
   ```
   断点：create() 方法 - 查看如何生成物理计划
   ```

3. **表扫描算子**：`src/observer/sql/operator/physical_operator/table_scan_physical_operator.cpp`
   ```
   断点1：TableScanPhysicalOperator::open() - 打开扫描器
   断点2：TableScanPhysicalOperator::next() - 每次获取下一行
   ```

4. **过滤算子**：`src/observer/sql/operator/physical_operator/predicate_physical_operator.cpp`
   ```
   断点1：PredicatePhysicalOperator::next() 函数开始
   断点2：filter_->evaluate() 调用处 - 检查条件是否满足
   ```

5. **投影算子**：`src/observer/sql/operator/physical_operator/project_physical_operator.cpp`
   ```
   断点：ProjectPhysicalOperator::next() - 选择需要的列
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
SELECT id, name FROM t1 WHERE id > 1;
```

**执行流程断点**：
```
1. SelectStmt::create()                      ← F9设置断点
   ├─ 验证表 t1 存在                          ← 检查 tables 变量
   ├─ 验证字段 id, name 存在                  ← 检查 query_expressions
   └─ 创建过滤条件 (id > 1)                   ← 检查 filter_stmt

2. PhysicalPlanGenerator::create()           ← F9设置断点
   └─ 生成算子树

3. ProjectPhysicalOperator::open()           ← F9设置断点
   └─ PredicatePhysicalOperator::open()      ← F9设置断点
       └─ TableScanPhysicalOperator::open()  ← F9设置断点，F11进入

4. 循环获取数据：
   ProjectPhysicalOperator::next()
     └─ PredicatePhysicalOperator::next()    ← 核心断点！
         ├─ child_->next()                   ← 获取下一行
         │   └─ TableScanPhysicalOperator::next()  ← 读取记录
         │       └─ record_scanner_.next()   ← F11 进入查看扫描
         │
         └─ filter_->evaluate()              ← 断点：评估 WHERE 条件
             └─ 返回 true/false              ← 检查返回值
```

**调试技巧 - 查看每一行数据**：
```cpp
// 在 TableScanPhysicalOperator::next() 中
RC TableScanPhysicalOperator::next()
{
  RC rc = record_scanner_.next(current_record_);  // ← 断点
  
  // 在这里添加条件断点，只在读到特定记录时中断
  // 右键断点 → 条件 → 输入：current_record_.rid().page_num == 1
  
  if (rc != RC::SUCCESS) {
    return rc;
  }
  
  tuple_.set_record(&current_record_);  // ← 检查 current_record_ 的内容
  // 展开 current_record_ → data_ → 查看二进制数据
  
  return rc;
}
```

**调试技巧 - 查看过滤条件**：
```cpp
// 在 PredicatePhysicalOperator::next() 中
RC PredicatePhysicalOperator::next()
{
  while (true) {
    RC rc = child_->next();  // ← 获取一行
    if (rc != RC::SUCCESS) {
      return rc;
    }
    
    Value value;
    rc = filter_->evaluate(tuple_, value);  // ← 断点：评估条件
    // F11 进入 evaluate 查看条件如何计算
    
    if (value.get_boolean()) {  // ← 检查 value，true表示满足条件
      return RC::SUCCESS;  // 返回这一行
    }
    // false 则继续循环，获取下一行
  }
}
```

#### 1.3 SELECT 实现

**查询流程图**：

```
SELECT * FROM t WHERE id > 1
         ↓
1. Parser: 解析为 SelectSqlNode
         ↓
2. Resolver: 创建 SelectStmt
   - 验证表存在
   - 验证字段存在
   - 构建过滤条件
         ↓
3. Optimizer: 生成执行计划
   ProjectPhysicalOperator          ← 投影
     └─ PredicatePhysicalOperator   ← 过滤 (id > 1)
         └─ TableScanPhysicalOperator ← 表扫描
         ↓
4. Executor: 执行
   - TableScan: 逐行读取
   - Predicate: 过滤不满足条件的行
   - Project: 选择需要的列
         ↓
5. 返回结果
```

**表扫描算子（table_scan_physical_operator.cpp）**：

```cpp
RC TableScanPhysicalOperator::open(Trx *trx)
{
  // 打开记录扫描器
  RC rc = table_->get_record_scanner(
      record_scanner_,     // 输出：扫描器对象
      trx,                 // 事务
      mode_                // 扫描模式
  );
  
  if (rc != RC::SUCCESS) {
    LOG_WARN("failed to open record scanner. rc=%s", strrc(rc));
  }
  return rc;
}

RC TableScanPhysicalOperator::next()
{
  // 从扫描器获取下一条记录
  RC rc = record_scanner_.next(current_record_);
  if (rc != RC::SUCCESS) {
    if (RC::RECORD_EOF != rc) {
      LOG_WARN("failed to scan record. rc=%s", strrc(rc));
    }
    return rc;
  }

  // 设置当前元组
  tuple_.set_record(&current_record_);
  return rc;
}
```

### 🐛 调试指南 - CREATE INDEX

#### 断点位置设置

**调试 CREATE INDEX 的关键断点**：

1. **执行器**：`src/observer/sql/executor/create_index_executor.cpp`
   ```
   断点：CreateIndexExecutor::execute() 函数开始
   ```

2. **表层**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::create_index() 函数开始
   断点2：验证字段存在的循环
   断点3：index->create() 调用处
   断点4：扫描表并插入索引的循环
   断点5：table_meta_.add_index() 调用处
   ```

3. **索引实现**：`src/observer/storage/index/bplus_tree_index.cpp`
   ```
   断点1：BplusTreeIndex::create() - 创建索引文件
   断点2：BplusTreeIndex::insert_entry() - 插入索引项
   ```

4. **B+树实现**：`src/observer/storage/index/bplus_tree.cpp`
   ```
   断点1：BplusTreeHandler::create() - 初始化B+树
   断点2：BplusTreeHandler::insert_entry() - B+树插入
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
CREATE INDEX idx_id ON t1(id);
```

**调用链追踪**：
```
CreateIndexExecutor::execute()
  └─ Table::create_index()                    ← F9 断点
      ├─ 验证字段 "id" 存在                    ← 检查 field_metas
      │
      ├─ IndexMeta::init()                    ← 初始化索引元数据
      │
      ├─ BplusTreeIndex::create()             ← F9 断点，F11进入
      │   └─ BplusTreeHandler::create()       ← 创建 B+树文件
      │
      ├─ 扫描表中的所有记录                     ← F9 断点（循环开始处）
      │   while (scanner.next(record)) {      ← 循环断点
      │     index->insert_entry(record)       ← 断点：每条记录
      │   }
      │
      └─ table_meta_.add_index()              ← 更新元数据
```

**重点调试 - 扫描表并建立索引**：
```cpp
// 在 Table::create_index() 中
RecordFileScanner scanner;
rc = get_record_scanner(scanner, trx, mode);

Record record;
int count = 0;  // 添加计数器方便观察
while (OB_SUCC(rc = scanner.next(record))) {  // ← 断点
  count++;
  
  // 从记录中提取索引键值
  rc = index->insert_entry(record.data(), &record.rid());  // ← F11进入
  
  // 添加条件断点：count == 100
  // 这样可以跳过前99条，直接看第100条
  
  if (rc != RC::SUCCESS) {
    // 如果某条记录插入失败（如唯一索引冲突）
    break;  // ← 断点：观察失败原因
  }
}
```

**重点调试 - B+树插入**：
```cpp
// 在 BplusTreeIndex::insert_entry() 中
RC BplusTreeIndex::insert_entry(const char *record, const RID *rid)
{
  // 1. 提取索引键
  char *key = new char[key_length_];  // ← 断点
  
  // 从记录中提取字段值
  for (const FieldMeta *field : field_metas_) {
    memcpy(key + offset, 
           record + field->offset(), 
           field->len());  // ← 检查 key 的内容
  }
  
  // 2. 插入 B+树
  RC rc = index_handler_.insert_entry(key, rid);  // ← F11 深入B+树
  
  delete[] key;
  return rc;
}
```

**调试技巧 - 可视化 B+树结构**：
在调试过程中，可以在 Watch 窗口添加：
```
index_handler_.file_header_  // 查看B+树的根节点页号
index_handler_.root_page_    // 查看根节点内容
```

#### 1.4 CREATE INDEX 实现

**执行器（create_index_executor.cpp）**：

```cpp
RC CreateIndexExecutor::execute(SQLStageEvent *sql_event)
{
  CreateIndexStmt *stmt = static_cast<CreateIndexStmt *>(sql_event->stmt());
  
  Table      *table       = stmt->table();
  const char *index_name  = stmt->index_name().c_str();
  const std::vector<std::string> &field_names = stmt->field_names();
  
  // 创建索引
  RC rc = table->create_index(
      nullptr,           // 事务
      false,             // 是否唯一索引
      index_name,        // 索引名
      field_names        // 字段名列表
  );
  
  return rc;
}
```

**表层实现（table.cpp）**：

```cpp
RC Table::create_index(Trx *trx, bool unique, const char *index_name,
                       const std::vector<std::string> &field_names)
{
  // 1. 验证字段是否存在
  std::vector<const FieldMeta *> field_metas;
  for (const std::string &field_name : field_names) {
    const FieldMeta *field_meta = table_meta_.field(field_name.c_str());
    if (nullptr == field_meta) {
      LOG_WARN("no such field in table. db=%s, table=%s, field=%s",
          base_dir_.c_str(), name(), field_name.c_str());
      return RC::SCHEMA_FIELD_NOT_EXIST;
    }
    field_metas.push_back(field_meta);
  }

  // 2. 创建索引元数据
  IndexMeta new_index_meta;
  RC rc = new_index_meta.init(index_name, field_metas, unique);
  if (rc != RC::SUCCESS) {
    return rc;
  }

  // 3. 创建B+树索引文件
  BplusTreeIndex *index = new BplusTreeIndex();
  std::string index_file = index_data_file(base_dir_.c_str(), name(), index_name);
  
  rc = index->create(this, index_file.c_str(), new_index_meta);
  if (rc != RC::SUCCESS) {
    delete index;
    return rc;
  }

  // 4. 扫描表，将已有数据插入索引
  RecordFileScanner scanner;
  rc = get_record_scanner(scanner, trx, mode);
  
  Record record;
  while (OB_SUCC(rc = scanner.next(record))) {
    rc = index->insert_entry(record.data(), &record.rid());
    if (rc != RC::SUCCESS) {
      break;
    }
  }

  // 5. 更新表元数据
  table_meta_.add_index(new_index_meta);
  indexes_.push_back(index);

  return rc;
}
```

---

## 2. insert - 多行插入

### 📝 功能概述

支持一条INSERT语句插入多行数据：
```sql
INSERT INTO t VALUES (1, 'a'), (2, 'b'), (3, 'c');
```

**特点**：
- ✅ 原子性：全部成功或全部失败
- ✅ 支持任意多行
- ✅ 失败自动回滚

### 🗂️ 关键文件

```
Parser:    yacc_sql.y (insert_value_list 规则)
Resolver:  insert_stmt.cpp
Executor:  insert_physical_operator.cpp
```

### 🐛 调试指南 - 多行插入

#### 断点位置设置

**调试多行插入的关键断点**：

1. **解析器**：查看生成的 `yacc_sql.cpp`
   ```
   断点：insert_value_list 规则的 action 代码
   ```

2. **执行器**：`src/observer/sql/operator/physical_operator/insert_physical_operator.cpp`
   ```
   断点1：InsertPhysicalOperator::open() 函数开始
   断点2：for (auto value : tuples_) 循环开始
   断点3：trx->insert_record() 成功后
   断点4：失败回滚的 for 循环（关键！）
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
INSERT INTO t1 VALUES (1, 'aaa'), (2, 'bbb'), (3, 'ccc');
```

**关键调试点 - 观察批量插入**：
```cpp
// 在 InsertPhysicalOperator::open() 中
RC InsertPhysicalOperator::open(Trx *trx)
{
  std::vector<Record *> inserted_record;  // ← 检查这个向量
  
  // ← 断点：检查 tuples_.size()，应该是 3
  for (auto value : tuples_) {  // 会循环3次
    Record record;
    
    // 第1次循环：插入 (1, 'aaa')
    // 第2次循环：插入 (2, 'bbb')
    // 第3次循环：插入 (3, 'ccc')
    
    RC rc = table_->make_record(
        static_cast<int>(value.size()), 
        value.data(), 
        record
    );
    
    if (rc != RC::SUCCESS) {
      return rc;  // ← 断点：make_record 失败
    }
    
    rc = trx->insert_record(table_, record);  // ← 断点
    
    if (rc != RC::SUCCESS) {
      // ← 关键断点！这里会触发回滚
      LOG_WARN("failed to insert record by transaction. rc=%s", strrc(rc));
      
      // 删除已成功插入的记录
      for (Record *rec : inserted_record) {  // ← 单步执行
        trx->delete_record(table_, *rec);  // 撤销之前的插入
      }
      return rc;
    }
    
    // 记录已插入的记录
    inserted_record.emplace_back(new Record(record));  // ← 检查大小
  }
  
  // 全部成功
  for (Record *&rec : inserted_record) {
    delete rec;
  }
  
  return RC::SUCCESS;
}
```

**测试回滚机制**：

**步骤1：准备测试场景**
```sql
-- 先创建表
CREATE TABLE t1(id int, name char(5));

-- 创建唯一索引
CREATE UNIQUE INDEX idx_id ON t1(id);

-- 插入一条数据
INSERT INTO t1 VALUES (1, 'aaa');
```

**步骤2：测试回滚**
```sql
-- 这条语句会失败（第2行与已有记录冲突）
INSERT INTO t1 VALUES (2, 'bbb'), (1, 'xxx'), (3, 'ccc');
```

**步骤3：F5调试观察**
```cpp
// 在 InsertPhysicalOperator::open() 中设置断点

循环第1次：
  插入 (2, 'bbb') → 成功
  inserted_record.size() = 1  // ← Watch 窗口查看

循环第2次：
  插入 (1, 'xxx') → 失败！（唯一索引冲突）
  进入回滚代码块  // ← 断点会停在这里
  
  for (Record *rec : inserted_record) {  // size = 1
    trx->delete_record(table_, *rec);    // ← F11 进入
    // 这里会删除第1次插入的 (2, 'bbb')
  }
  
  return RC::INDEX_KEY_DUPLICATE;

循环第3次：
  不会执行（已经返回失败）
```

**Watch 窗口观察**：
```
tuples_.size()              // 应该是 3
inserted_record.size()      // 随循环增长：0 → 1 → 2 → ...
value                       // 当前正在插入的值
rc                          // 返回码
```

### 💻 代码详解

#### 2.1 语法解析

**yacc_sql.y**：

```yacc
insert_stmt:
    INSERT INTO ID VALUES insert_value_list {
        $$ = new ParsedSqlNode(SCF_INSERT);
        $$->insertion.relation_name = $3;
        
        // $5 是一个 vector<vector<Value>>
        // 外层vector代表多行，内层vector代表每行的多个字段
        if ($5 != nullptr) {
            $$->insertion.tuples.swap(*$5);
            delete $5;
        }
        free($3);
    }
    ;

// 多行值列表：(1,'a'), (2,'b'), (3,'c')
insert_value_list:
    LBRACE value value_list RBRACE {
        // 第一行
        $$ = new std::vector<std::vector<Value>>;
        std::vector<Value> values;
        
        if ($3 != nullptr) {
            values.swap(*$3);
            delete $3;
        }
        values.emplace_back(*$2);
        delete $2;
        
        std::reverse(values.begin(), values.end());
        $$->emplace_back(values);
    }
    | insert_value_list COMMA LBRACE value value_list RBRACE {
        // 后续行
        std::vector<Value> values;
        
        if ($5 != nullptr) {
            values.swap(*$5);
            delete $5;
        }
        values.emplace_back(*$4);
        delete $4;
        
        std::reverse(values.begin(), values.end());
        $$ = $1;
        $$->emplace_back(values);
    }
    ;
```

#### 2.2 Stmt 验证

**insert_stmt.cpp**：

```cpp
RC InsertStmt::create(Db *db, const InsertSqlNode &inserts, Stmt *&stmt)
{
  const char *table_name = inserts.relation_name.c_str();
  
  // 1. 检查表是否存在
  Table *table = db->find_table(table_name);
  if (nullptr == table) {
    LOG_WARN("no such table. db=%s, table_name=%s", 
             db->name(), table_name);
    return RC::SCHEMA_TABLE_NOT_EXIST;
  }

  // 2. 遍历每个需要插入的元组（多行）
  for (auto &values : inserts.tuples) {
    const int value_num = static_cast<int>(values.size());
    const TableMeta &table_meta = table->table_meta();
    const int field_num = table_meta.field_num() - table_meta.sys_field_num();

    // 3. 检查字段数量
    if (field_num != value_num) {
      LOG_WARN("schema mismatch. value num=%d, field num in schema=%d", 
               value_num, field_num);
      return RC::SCHEMA_FIELD_MISSING;
    }

    // 4. 检查每个字段的类型
    const int sys_field_num = table_meta.sys_field_num();
    for (int i = 0; i < field_num; i++) {
      const FieldMeta *field_meta = table_meta.field(i + sys_field_num);
      const AttrType field_type = field_meta->type();
      const AttrType value_type = values[i].attr_type();
      
      // 类型不匹配，尝试转换
      if (value_type != field_type) {
        Value real_value;
        
        // 5. 特殊处理：NULL值
        if (values[i].is_null()) {
          if (not field_meta->nullable()) {
            return RC::NULL_CANT_INSERT;  // 字段不允许NULL
          }
        } else {
          // 6. 类型转换
          RC rc = Value::cast_to(values[i], field_meta->type(), real_value);
          if (OB_FAIL(rc)) {
            LOG_WARN("failed to cast value. table:%s, field:%s, value:%s",
                table_meta.name(), field_meta->name(), 
                values[i].to_string().c_str());
            return rc;
          }
        }
      }
    }
  }

  // 7. 创建 InsertStmt（包含所有元组）
  stmt = new InsertStmt(table, inserts.tuples);
  return RC::SUCCESS;
}
```

#### 2.3 执行插入（原子性保证）

**insert_physical_operator.cpp**：

```cpp
RC InsertPhysicalOperator::open(Trx *trx)
{
  std::vector<Record *> inserted_record;
  
  // 遍历所有元组（可能是多行）
  for (auto value : tuples_) {
    Record record;
    
    // 1. 创建记录
    RC rc = table_->make_record(
        static_cast<int>(value.size()), 
        value.data(), 
        record
    );
    
    if (rc != RC::SUCCESS) {
      LOG_WARN("failed to make record. rc=%s", strrc(rc));
      return rc;
    }
    
    // 2. 插入记录（通过事务）
    rc = trx->insert_record(table_, record);
    
    if (rc != RC::SUCCESS) {
      LOG_WARN("failed to insert record by transaction. rc=%s", strrc(rc));
      
      // 3. 失败回滚：删除本次批量插入中已成功的记录
      // 这保证了原子性：要么全部成功，要么全部失败
      for (Record *rec : inserted_record) {
        trx->delete_record(table_, *rec);
      }
      return rc;
    }
    
    // 4. 记录已插入的记录（用于可能的回滚）
    inserted_record.emplace_back(new Record(record));
  }
  
  // 5. 全部成功，清理临时记录对象
  for (Record *&rec : inserted_record) {
    delete rec;
  }
  
  return RC::SUCCESS;
}
```

**关键点**：
- 使用 `inserted_record` 向量记录已插入的记录
- 一旦某一行插入失败，立即回滚所有已插入的记录
- 这保证了原子性

---

## 3. select-meta - 元数据检查

### 📝 功能概述

验证查询中的表名、字段名是否存在，不存在则返回FAILURE。

**示例**：
```sql
SELECT nonexist_col FROM t;        -- FAILURE: 字段不存在
SELECT * FROM nonexist_table;      -- FAILURE: 表不存在
SELECT t1.id FROM t2;              -- FAILURE: 表名不匹配
```

### 🗂️ 关键文件

```
Resolver: select_stmt.cpp (验证逻辑)
```

### 🐛 调试指南 - 元数据检查

#### 断点位置设置

**调试元数据验证的关键断点**：

1. **SelectStmt**：`src/observer/sql/stmt/select_stmt.cpp`
   ```
   断点1：SelectStmt::create() 函数开始
   断点2：验证表存在的循环
   断点3：验证字段存在的循环
   断点4：FilterStmt::create() 调用处（验证WHERE条件）
   ```

2. **ExpressionBinder**：`src/observer/sql/expr/expression_binder.cpp`
   ```
   断点1：bind_field() 函数开始
   断点2：查找表的代码
   断点3：查找字段的代码
   断点4：返回错误码的位置
   ```

#### F5 调试步骤

**测试场景1：表不存在**
```sql
SELECT * FROM nonexist_table;
```

**调试步骤**：
```cpp
// 在 SelectStmt::create() 中
RC SelectStmt::create(Db *db, SelectSqlNode &select_sql, Stmt *&stmt)
{
  // 验证表是否存在
  for (const std::string &table_name : select_sql.relations) {  // ← 断点
    // 检查 table_name = "nonexist_table"
    
    Table *table = db->find_table(table_name.c_str());  // ← F11进入
    
    if (nullptr == table) {  // ← 断点会停在这里
      // table 是 nullptr
      LOG_WARN("no such table. db=%s, table_name=%s", 
               db->name(), table_name.c_str());
      return RC::SCHEMA_TABLE_NOT_EXIST;  // ← 返回错误
    }
    
    tables.push_back(table);
  }
  // ...
}
```

**测试场景2：字段不存在**
```sql
SELECT nonexist_col FROM t1;
```

**调试步骤**：
```cpp
// 在 ExpressionBinder::bind_field() 中
RC ExpressionBinder::bind_field(
    const RelAttrSqlNode &attr, 
    Expression *&expr)
{
  const char *field_name = attr.attribute_name.c_str();  // ← "nonexist_col"
  
  // 在表中查找字段
  const FieldMeta *field_meta = table->table_meta().field(field_name);  // ← 断点
  
  if (nullptr == field_meta) {  // ← 会进入这里
    LOG_WARN("no such field in table. table=%s, field=%s", 
             table->name(), field_name);
    return RC::SCHEMA_FIELD_NOT_EXIST;  // ← 返回错误
  }
  
  // ...
}
```

**测试场景3：表名不匹配**
```sql
SELECT t1.id FROM t2;
```

**调试步骤**：
```cpp
// 在 ExpressionBinder::bind_field() 中
RC ExpressionBinder::bind_field(...)
{
  const char *table_name = attr.relation_name.c_str();  // ← "t1"
  const char *field_name = attr.attribute_name.c_str(); // ← "id"
  
  // 指定了表名，直接查找
  table = find_table(table_name);  // ← 断点
  // 在 tables_ 中查找 "t1"，但 tables_ 只有 "t2"
  
  if (nullptr == table) {  // ← 会进入这里
    LOG_WARN("no such table. table=%s", table_name);
    return RC::SCHEMA_TABLE_NOT_EXIST;  // ← 返回错误
  }
  
  // ...
}
```

**Watch 窗口观察**：
```
select_sql.relations        // 查询的表列表
tables                      // 找到的表对象
attr.relation_name          // 字段的表名限定
attr.attribute_name         // 字段名
field_meta                  // 找到的字段元数据（nullptr表示未找到）
```

**调试技巧 - 快速定位验证点**：

在 VSCode 的 `launch.json` 中添加条件断点：
```json
{
  "name": "调试元数据检查",
  "type": "cppdbg",
  "request": "launch",
  "program": "${workspaceFolder}/build/bin/observer",
  "stopAtEntry": false,
  "breakpoints": [
    {
      "file": "select_stmt.cpp",
      "line": 100,
      "condition": "table == nullptr"
    }
  ]
}
```

### 💻 代码详解

**select_stmt.cpp**：

```cpp
RC SelectStmt::create(Db *db, SelectSqlNode &select_sql, Stmt *&stmt)
{
  // 1. 验证表是否存在
  std::vector<Table *> tables;
  for (const std::string &table_name : select_sql.relations) {
    Table *table = db->find_table(table_name.c_str());
    if (nullptr == table) {
      LOG_WARN("no such table. db=%s, table_name=%s", 
               db->name(), table_name.c_str());
      return RC::SCHEMA_TABLE_NOT_EXIST;  // ← 返回错误
    }
    tables.push_back(table);
  }

  // 2. 验证查询字段
  std::vector<std::unique_ptr<Expression>> query_expressions;
  for (const SelectAttr &select_attr : select_sql.attributes) {
    
    // 处理 * 的情况
    if (select_attr.is_star) {
      // ... 展开为所有字段
    } else {
      // 3. 验证字段是否存在
      Expression *expr = nullptr;
      RC rc = create_expression(
          select_attr,      // 字段描述
          tables,           // 表列表
          expr,             // 输出：表达式对象
          default_table     // 默认表
      );
      
      if (rc != RC::SUCCESS) {
        LOG_WARN("failed to create expression. rc=%s", strrc(rc));
        return rc;  // ← 字段不存在，返回错误
      }
      
      query_expressions.emplace_back(expr);
    }
  }

  // 4. 验证 WHERE 条件中的字段
  FilterStmt *filter_stmt = nullptr;
  RC rc = FilterStmt::create(
      db, default_table, tables.data(), tables.size(),
      select_sql.conditions, filter_stmt
  );
  
  if (rc != RC::SUCCESS) {
    LOG_WARN("failed to create filter statement. rc=%s", strrc(rc));
    return rc;  // ← WHERE中的字段不存在
  }

  // 5. 所有验证通过，创建 SelectStmt
  stmt = new SelectStmt();
  return RC::SUCCESS;
}
```

**字段验证（expression_binder.cpp）**：

```cpp
RC ExpressionBinder::bind_field(
    const RelAttrSqlNode &attr, 
    Expression *&expr)
{
  const char *table_name = attr.relation_name.c_str();
  const char *field_name = attr.attribute_name.c_str();

  Table *table = nullptr;
  
  // 1. 查找表
  if (common::is_blank(table_name)) {
    // 没有指定表名，在所有表中查找
    for (Table *t : tables_) {
      const FieldMeta *field_meta = t->table_meta().field(field_name);
      if (field_meta != nullptr) {
        if (table != nullptr) {
          // 歧义：多个表都有这个字段
          LOG_WARN("ambiguous field. field=%s", field_name);
          return RC::SCHEMA_FIELD_NAME_DUPLICATE;
        }
        table = t;
      }
    }
  } else {
    // 指定了表名，直接查找
    table = find_table(table_name);
    if (nullptr == table) {
      LOG_WARN("no such table. table=%s", table_name);
      return RC::SCHEMA_TABLE_NOT_EXIST;  // ← 表不存在
    }
  }

  if (nullptr == table) {
    LOG_WARN("no such field. field=%s", field_name);
    return RC::SCHEMA_FIELD_NOT_EXIST;  // ← 字段不存在
  }

  // 2. 在表中查找字段
  const FieldMeta *field_meta = table->table_meta().field(field_name);
  if (nullptr == field_meta) {
    LOG_WARN("no such field in table. table=%s, field=%s", 
             table->name(), field_name);
    return RC::SCHEMA_FIELD_NOT_EXIST;  // ← 字段不存在
  }

  // 3. 创建字段表达式
  expr = new FieldExpr(table, field_meta);
  return RC::SUCCESS;
}
```

**关键点**：
- 在 Resolver 阶段就进行严格验证
- 不存在的表名或字段名会立即返回错误码
- 错误码传播到上层，最终返回 FAILURE

---

## 4. drop-table - 删除表

### 📝 功能概述

删除表及其所有关联资源：
- 表数据文件
- 所有索引文件
- 表元数据文件

### 🗂️ 关键文件

```
Executor: drop_table_executor.cpp
Storage:  table.cpp, db.cpp
```

### 🐛 调试指南 - DROP TABLE

#### 断点位置设置

**调试 DROP TABLE 的关键断点**：

1. **执行器**：`src/observer/sql/executor/drop_table_executor.cpp`
   ```
   断点：DropTableExecutor::execute() 函数开始
   ```

2. **数据库层**：`src/observer/storage/db/db.cpp`
   ```
   断点1：Db::drop_table() 函数开始
   断点2：table->close() 调用前
   断点3：table->destroy() 调用前
   断点4：从 opened_tables_ 移除的代码
   ```

3. **表层**：`src/observer/storage/table/table.cpp`
   ```
   断点1：Table::destroy() 函数开始
   断点2：删除索引文件的循环
   断点3：unlink(data_file) 调用处
   断点4：unlink(meta_file) 调用处
   ```

#### F5 调试步骤

**测试 SQL**：
```sql
DROP TABLE t1;
```

**调用链追踪**：
```
DropTableExecutor::execute()
  └─ Db::drop_table("t1")                     ← F9 断点
      ├─ find_table("t1")                     ← 检查表是否存在
      │
      ├─ table->close()                       ← F9 断点
      │   └─ 关闭所有文件句柄
      │
      ├─ table->destroy()                     ← F9 断点，F11 进入
      │   ├─ 删除所有索引文件                  ← 循环断点
      │   │   for (Index *index : indexes_) {
      │   │     unlink(index_file)            ← 检查文件路径
      │   │   }
      │   │
      │   ├─ unlink(data_file)                ← 断点：删除数据文件
      │   │   检查返回值：0表示成功
      │   │
      │   └─ unlink(meta_file)                ← 断点：删除元数据文件
      │
      ├─ 从 opened_tables_ 移除               ← 检查容器变化
      │
      └─ delete table                         ← 释放内存
```

**重点调试 - 删除所有文件**：
```cpp
// 在 Table::destroy() 中
RC Table::destroy()
{
  // 1. 删除所有索引文件
  for (Index *index : indexes_) {  // ← 断点：检查 indexes_.size()
    
    std::string index_file = index_data_file(
        base_dir_.c_str(), 
        name(), 
        index->index_meta().name()
    );
    
    // ← 断点：查看 index_file 的完整路径
    // 例如：/tmp/miniob/data/sys/t1.idx_id.index
    
    if (0 != ::unlink(index_file.c_str())) {  // ← 断点
      // unlink 返回 -1 表示失败
      // 检查 errno 的值
      LOG_ERROR("failed to remove index file. filename=%s, errmsg=%s", 
                index_file.c_str(), strerror(errno));
      return RC::IOERR_DELETE;
    }
    
    delete index;  // ← 释放索引对象
  }
  indexes_.clear();  // ← 检查 size() 变为 0

  // 2. 删除数据文件
  std::string data_file = table_data_file(base_dir_.c_str(), name());
  // ← 断点：例如 /tmp/miniob/data/sys/t1.table
  
  if (0 != ::unlink(data_file.c_str())) {  // ← 断点
    LOG_ERROR("failed to remove data file. filename=%s, errmsg=%s", 
              data_file.c_str(), strerror(errno));
    return RC::IOERR_DELETE;
  }

  // 3. 删除元数据文件
  std::string meta_file = table_meta_file(base_dir_.c_str(), name());
  // ← 断点：例如 /tmp/miniob/data/sys/t1.table-meta
  
  if (0 != ::unlink(meta_file.c_str())) {  // ← 断点
    LOG_ERROR("failed to remove meta file. filename=%s, errmsg=%s", 
              meta_file.c_str(), strerror(errno));
    return RC::IOERR_DELETE;
  }

  LOG_INFO("table destroyed. table=%s", name());
  return RC::SUCCESS;
}
```

**Watch 窗口观察**：
```
indexes_.size()             // 索引数量
index_file                  // 当前删除的索引文件路径
data_file                   // 数据文件路径
meta_file                   // 元数据文件路径
errno                       // 如果删除失败，查看错误码
```

**调试技巧 - 验证文件确实被删除**：

在删除前后使用文件浏览器或命令行查看：
```bash
# 在另一个终端监控文件变化
watch -n 1 'ls -lh /tmp/miniob/data/sys/'
```

或在调试器中设置后置断点：
```cpp
// 在 unlink() 之后
if (0 == ::unlink(data_file.c_str())) {
  // ← 断点：此时文件已被删除
  // 可以在这里添加日志或验证代码
}
```

**常见问题调试**：

1. **文件删除失败** (errno == ENOENT)
   - 文件不存在
   - 检查文件路径是否正确

2. **文件删除失败** (errno == EACCES)
   - 权限不足
   - 检查文件权限和目录权限

3. **表仍在使用中**
   - 断点在 `table->close()` 查看是否正确关闭

### 💻 代码详解

**drop_table_executor.cpp**：

```cpp
RC DropTableExecutor::execute(SQLStageEvent *sql_event)
{
  Stmt    *stmt    = sql_event->stmt();
  Session *session = sql_event->session_event()->session();
  ASSERT(stmt->type() == StmtType::DROP_TABLE, 
         "drop table executor can not run this command: %d", 
         static_cast<int>(stmt->type()));

  DropTableStmt *drop_table_stmt = static_cast<DropTableStmt *>(stmt);

  const char *table_name = drop_table_stmt->table_name().c_str();
  
  // 从数据库中删除表
  RC rc = session->get_current_db()->drop_table(table_name);

  return rc;
}
```

**db.cpp**：

```cpp
RC Db::drop_table(const char *table_name)
{
  // 1. 查找表
  Table *table = find_table(table_name);
  if (nullptr == table) {
    LOG_WARN("no such table. table_name=%s", table_name);
    return RC::SCHEMA_TABLE_NOT_EXIST;
  }

  // 2. 关闭表（释放资源）
  table->close();

  // 3. 删除表的所有文件
  RC rc = table->destroy();
  if (rc != RC::SUCCESS) {
    LOG_ERROR("failed to destroy table. table_name=%s, rc=%s", 
              table_name, strrc(rc));
    return rc;
  }

  // 4. 从内存中移除表对象
  auto iter = std::find_if(opened_tables_.begin(), opened_tables_.end(),
      [table_name](const Table *t) { 
          return 0 == strcmp(t->name(), table_name); 
      });
  
  if (iter != opened_tables_.end()) {
    opened_tables_.erase(iter);
  }

  // 5. 删除表对象
  delete table;

  LOG_INFO("dropped table. table_name=%s", table_name);
  return RC::SUCCESS;
}
```

**table.cpp**：

```cpp
RC Table::destroy()
{
  // 1. 删除所有索引文件
  for (Index *index : indexes_) {
    // 获取索引文件路径
    std::string index_file = index_data_file(
        base_dir_.c_str(), 
        name(), 
        index->index_meta().name()
    );
    
    // 删除索引文件
    if (0 != ::unlink(index_file.c_str())) {
      LOG_ERROR("failed to remove index file. filename=%s, errmsg=%s", 
                index_file.c_str(), strerror(errno));
      return RC::IOERR_DELETE;
    }
    
    delete index;
  }
  indexes_.clear();

  // 2. 删除数据文件
  std::string data_file = table_data_file(base_dir_.c_str(), name());
  if (0 != ::unlink(data_file.c_str())) {
    LOG_ERROR("failed to remove data file. filename=%s, errmsg=%s", 
              data_file.c_str(), strerror(errno));
    return RC::IOERR_DELETE;
  }

  // 3. 删除元数据文件
  std::string meta_file = table_meta_file(base_dir_.c_str(), name());
  if (0 != ::unlink(meta_file.c_str())) {
    LOG_ERROR("failed to remove meta file. filename=%s, errmsg=%s", 
              meta_file.c_str(), strerror(errno));
    return RC::IOERR_DELETE;
  }

  LOG_INFO("table destroyed. table=%s", name());
  return RC::SUCCESS;
}
```

**关键点**：
- 必须删除表的所有关联文件
- 删除顺序：索引文件 → 数据文件 → 元数据文件
- 删除前需要关闭文件句柄

---

## 🔧 通用调试场景

### 场景1：SQL 执行失败，如何快速定位？

**步骤1：查看日志**
```bash
tail -f observer.log | grep ERROR
```

**步骤2：在返回错误码的地方设置断点**
```cpp
// 搜索代码中的 return RC::xxx (非SUCCESS)
// 在所有返回错误的地方设置断点
if (rc != RC::SUCCESS) {  // ← F9 设置断点
  return rc;
}
```

**步骤3：查看调用栈**
- 错误发生时，在调试面板查看完整调用链
- 从上往下逐层分析，找到根本原因

### 场景2：数据插入后查询不到，如何调试？

**断点设置**：
```
1. InsertPhysicalOperator::open()        ← 确认插入成功
2. Table::insert_record()                ← 确认写入文件
3. Index::insert_entry()                 ← 确认索引更新
4. TableScanPhysicalOperator::next()     ← 确认能读取
```

**检查清单**：
```cpp
// 在 insert_record() 中
- 检查 record.rid() 是否有效（page_num > 0）
- 检查 record_handler_->insert_record() 返回值
- 检查索引是否更新成功

// 在 next() 中
- 检查 record_scanner_.next() 是否返回 SUCCESS
- 检查 current_record_ 的数据是否正确
```

### 场景3：程序崩溃，如何调试？

**方法1：启用核心转储**
```bash
ulimit -c unlimited
./observer
# 崩溃后
gdb ./observer core
bt  # 查看崩溃时的调用栈
```

**方法2：使用 AddressSanitizer**
```bash
cd build
cmake -DCMAKE_BUILD_TYPE=Debug -DENABLE_ASAN=ON ..
make
./bin/observer
# 会自动检测内存错误并报告
```

**方法3：在可能崩溃的地方设置断点**
```cpp
// 空指针解引用
if (ptr == nullptr) {  // ← 设置断点
  // 崩溃前会停在这里
}

// 数组越界
for (int i = 0; i < size; i++) {  // ← 设置条件断点: i >= real_size
  array[i] = ...;
}
```

### 场景4：性能问题，如何分析？

**方法1：使用计时断点**
```cpp
// 在函数开始和结束处设置日志断点
// 开始处：开始执行 {函数名}
// 结束处：结束执行 {函数名}
// 通过日志时间戳计算耗时
```

**方法2：使用性能分析工具**
```bash
# gprof
g++ -pg ...
./observer
gprof observer gmon.out

# perf (Linux)
perf record -g ./observer
perf report
```

**方法3：手动添加计时代码**
```cpp
#include <chrono>

auto start = std::chrono::high_resolution_clock::now();
// 要测量的代码
auto end = std::chrono::high_resolution_clock::now();
auto duration = std::chrono::duration_cast<std::chrono::microseconds>(end - start);
LOG_INFO("耗时: %ld 微秒", duration.count());
```

### 场景5：并发问题，如何调试？

**断点技巧**：
```cpp
// 在锁操作处设置断点
mutex.lock();     // ← 断点：观察哪个线程获得锁
// 临界区
mutex.unlock();   // ← 断点：观察何时释放锁
```

**多线程调试**：
- 在调试面板的"线程"视图查看所有线程
- 右键线程可以冻结/解冻特定线程
- 使用条件断点只在特定线程中断：`线程ID == 2`

### 场景6：内存泄漏，如何检测？

**使用 Valgrind**：
```bash
valgrind --leak-check=full --show-leak-kinds=all ./observer
```

**在代码中追踪**：
```cpp
// 在对象创建和销毁处设置断点
Table *table = new Table();  // ← 计数器 +1
delete table;                // ← 计数器 -1
// 程序结束时计数器应该为 0
```

## 🎯 调试最佳实践

### 1. 自顶向下调试法
```
SQL 输入
  ↓ ParseStage         ← 先确认这层正确
  ↓ ResolveStage       ← 再确认这层正确
  ↓ OptimizeStage      ← 再确认这层正确
  ↓ ExecuteStage       ← 最后到这层
  ↓ 结果输出
```

### 2. 二分查找法
- 在函数开始和结束设置断点
- 确认输入正确、输出错误 → 问题在函数内部
- 确认输入错误 → 问题在调用方
- 在函数中间设置断点，逐步缩小范围

### 3. 对比调试法
- 同时运行正确和错误的用例
- 在相同位置设置断点
- 对比变量值的差异

### 4. 日志辅助法
```cpp
// 在关键位置添加日志
LOG_DEBUG("进入函数: %s, 参数: %d", __FUNCTION__, param);
LOG_DEBUG("变量值: %d", value);
LOG_DEBUG("返回值: %s", strrc(rc));
```

### 5. 断言检查法
```cpp
// 在预期条件处添加断言
ASSERT(ptr != nullptr, "指针不应为空");
ASSERT(size > 0, "大小应大于0");
ASSERT(rc == RC::SUCCESS, "操作应该成功");
```

## 📝 调试检查清单

**开始调试前**：
- [ ] 代码已用 Debug 模式编译
- [ ] 日志级别设置为 DEBUG
- [ ] 了解要调试的功能的代码流程
- [ ] 准备好测试 SQL 语句

**调试过程中**：
- [ ] 设置断点在关键函数入口
- [ ] 检查函数参数是否正确
- [ ] 单步执行，观察变量变化
- [ ] 检查返回值是否符合预期
- [ ] 查看调用栈，理解执行路径

**调试完成后**：
- [ ] 理解了问题的根本原因
- [ ] 验证了修复方案
- [ ] 添加了相应的测试用例
- [ ] 更新了相关文档

---

由于篇幅限制，我将创建一个第二部分继续讲解剩余功能。让我继续：

